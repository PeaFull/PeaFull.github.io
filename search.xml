<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>20180504随笔</title>
    <url>/2018/05/04/20180504Essay/</url>
    <content><![CDATA[<h3 id="3月份的时候了解到了Hexo，感觉还是挺有意思的，主要是很多东西需要自己折腾，而且比博客园要好看很多，就折腾了下；"><a href="#3月份的时候了解到了Hexo，感觉还是挺有意思的，主要是很多东西需要自己折腾，而且比博客园要好看很多，就折腾了下；" class="headerlink" title="3月份的时候了解到了Hexo，感觉还是挺有意思的，主要是很多东西需要自己折腾，而且比博客园要好看很多，就折腾了下；"></a>3月份的时候了解到了Hexo，感觉还是挺有意思的，主要是很多东西需要自己折腾，而且比博客园要好看很多，就折腾了下；</h3><span id="more"></span>
<h3 id="其实很久都不更新博客了，3月至5月这个项目确实也没啥要记的，都是业务逻辑，也可能是自己越来越懒了，以后要养成好习惯，不论是知识点，难点还是开发中的报错，都要记录一下；"><a href="#其实很久都不更新博客了，3月至5月这个项目确实也没啥要记的，都是业务逻辑，也可能是自己越来越懒了，以后要养成好习惯，不论是知识点，难点还是开发中的报错，都要记录一下；" class="headerlink" title="其实很久都不更新博客了，3月至5月这个项目确实也没啥要记的，都是业务逻辑，也可能是自己越来越懒了，以后要养成好习惯，不论是知识点，难点还是开发中的报错，都要记录一下；"></a>其实很久都不更新博客了，3月至5月这个项目确实也没啥要记的，都是业务逻辑，也可能是自己越来越懒了，以后要养成好习惯，不论是知识点，难点还是开发中的报错，都要记录一下；</h3><h3 id="得抓紧时间落实一下FD的事情；"><a href="#得抓紧时间落实一下FD的事情；" class="headerlink" title="得抓紧时间落实一下FD的事情；"></a>得抓紧时间落实一下FD的事情；</h3><h3 id="没事就练练车，提高下车技；"><a href="#没事就练练车，提高下车技；" class="headerlink" title="没事就练练车，提高下车技；"></a>没事就练练车，提高下车技；</h3>]]></content>
      <categories>
        <category>生活</category>
      </categories>
      <tags>
        <tag>Life</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo使用小记</title>
    <url>/2018/05/04/Hexo/</url>
    <content><![CDATA[<h3 id="1-关于部署到github时用户名密码报错的问题"><a href="#1-关于部署到github时用户名密码报错的问题" class="headerlink" title="1.  关于部署到github时用户名密码报错的问题"></a>1.  关于部署到github时用户名密码报错的问题</h3><p>刚开始部署的配置如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">deploy:   </span><br><span class="line">    type: git   </span><br><span class="line">    repo: https://github.com/&#123;yourname&#125;/&#123;yourname&#125;.github.io.git   </span><br><span class="line">    branch: master</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>结果会报没有用户名密码的错误，大致如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fatal: could not read Username for &#x27;GitHub · Where software is built&#x27;: No error</span><br><span class="line">FATAL Something&#x27;s wrong. Maybe you can find the solution here: Troubleshooting</span><br><span class="line">Error: bash: /dev/tty: No such device or address</span><br><span class="line">error: failed to execute prompt script (exit code 1)</span><br><span class="line">fatal: could not read Username for &#x27;GitHub · Where software is built&#x27;: No erro</span><br></pre></td></tr></table></figure>
<p>解决办法为修改配置为如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">deploy:    </span><br><span class="line">    type: git   </span><br><span class="line">    repo: https://&#123;yourname&#125;:&#123;yourpassword&#125;@github.com/&#123;yourname&#125;/&#123;yourname&#125;.github.io.git</span><br><span class="line">    branch: master</span><br></pre></td></tr></table></figure>
<p>转自：<a href="https://www.zhihu.com/question/38219432">有关使用 Hexo 和 GitHub 搭建博客，出现 hexo -d 报错如何解决？</a></p>
<h3 id="2-配置文件一定要指明language"><a href="#2-配置文件一定要指明language" class="headerlink" title="2.  配置文件一定要指明language"></a>2.  配置文件一定要指明language</h3><span id="more"></span>
<h3 id="3-集成disqus评论功能"><a href="#3-集成disqus评论功能" class="headerlink" title="3.  集成disqus评论功能"></a>3.  集成disqus评论功能</h3><p>进入disqus官网，点击红色框中链接，给自己的网站添加disqus，如下图</p>
<img src="/2018/05/04/Hexo/disqus_create_new.png" class="" title="disqus_create_new">
<p>接着输入你的网址，选择类别，生成shortname</p>
<img src="/2018/05/04/Hexo/disqus_form.png" class="" title="disqus_form">
<p>拿到shortname，分别添加到站点配置文件的disqus_shortname和主题配置文件的disqus中的shortname，并且将主题配置文件disqus的enable设为true</p>
<h3 id="4-如何给文章的md文件中插入图片"><a href="#4-如何给文章的md文件中插入图片" class="headerlink" title="4.  如何给文章的md文件中插入图片"></a><del>4.  如何给文章的md文件中插入图片</del></h3><s>
把站点配置文件的post_asset_folder设置为true；

<p>接着执行命令，来自<a href="https://github.com/CodeFalling/hexo-asset-image">hexo-asset-image</a>；</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ npm install hexo-asset-image --save</span><br></pre></td></tr></table></figure>

<p>安装好之后，在用<code>hexo n xxx</code>创建新文档的时候，&#x2F;source&#x2F;_posts文件夹内除了xxx.md文件还会有一个同名的文件夹，这个文件夹就是用来存放图片的；</p>
<p>最后在xxx.md中想引入图片时先，先把图片复制到xxx文件夹中，接下来直接在xxx.md中按照markdown的格式引入图片：<code>![你想输入的替代文字](xxxx/图片名.jpg)</code>即可</p>
<p>转自：<a href="https://blog.csdn.net/sugar_rainbow/article/details/57415705">hexo生成博文插入图片</a><br></s></p>
<h3 id="5-设置阅读全文"><a href="#5-设置阅读全文" class="headerlink" title="5.  设置阅读全文"></a>5.  设置阅读全文</h3><p>根据文章的内容，自己在合适的位置添加 &lt;!–more–&gt;标签，使用灵活，也是Hexo推荐的方法</p>
<h3 id="6-使用local-search为站点增加搜索功能"><a href="#6-使用local-search为站点增加搜索功能" class="headerlink" title="6.  使用local_search为站点增加搜索功能"></a>6.  使用local_search为站点增加搜索功能</h3><p>安装 hexo-generator-search，在站点的根目录下执行以下命令：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ npm install hexo-generator-search --save</span><br></pre></td></tr></table></figure>
<p>安装 hexo-generator-searchdb，在站点的根目录下执行以下命令：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ npm install hexo-generator-searchdb --save</span><br></pre></td></tr></table></figure>
<p>编辑 站点配置文件，新增以下内容到任意位置：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">search:</span><br><span class="line">  path: search.xml</span><br><span class="line">  field: post</span><br><span class="line">  format: html</span><br><span class="line">  limit: 10000</span><br></pre></td></tr></table></figure>
<p>修改主题配置文件中local_search的enable为true即可</p>
<p>转自：<a href="https://blog.csdn.net/qq_40265501/article/details/80030627">hexo博客添加搜索功能</a></p>
<h3 id="持续补充中。。。"><a href="#持续补充中。。。" class="headerlink" title="持续补充中。。。"></a>持续补充中。。。</h3>]]></content>
      <categories>
        <category>编程</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>Chrome浏览器插件开发</title>
    <url>/2024/07/13/chromeExtensions/</url>
    <content><![CDATA[<h4 id="1-项目目录结构："><a href="#1-项目目录结构：" class="headerlink" title="1. 项目目录结构："></a>1. 项目目录结构：</h4><ul>
<li><strong>manifest.json：插件的配置文件</strong><ol>
<li>每个扩展程序的根目录（必须）中都必须包含一个 manifest.json 文件</li>
<li>文档：<a href="https://developer.chrome.com/docs/extensions/reference/manifest?hl=zh-cn#minimal-manifest">https://developer.chrome.com/docs/extensions/reference/manifest?hl=zh-cn#minimal-manifest</a><span id="more"></span></li>
</ol>
</li>
<li><strong>service worker</strong><ol>
<li>处理和监听浏览器事件，相当于在后台运行的脚本，可以使用浏览器的全部api，但是不能和页面内容直接交互</li>
<li>文档：<a href="https://developer.chrome.com/docs/extensions/get-started/tutorial/service-worker-events?hl=zh-cn">https://developer.chrome.com/docs/extensions/get-started/tutorial/service-worker-events?hl=zh-cn</a></li>
<li>由于mv3版本的service_worker在五分钟后就会终止进程，其中所有的状态和定时任务都将丢失。所以不要再service_worker中直接定义变量和使用setTimeout、setInterval等定时任务。改用chrome.storage储存需要的变量。使用chrome.alarm来执行定时任务。</li>
</ol>
</li>
<li><strong>Content scripts</strong><ol>
<li>在网页环境中运行的文件。通过使用标准文档对象模型 (DOM)，用户能够读取浏览器所访问网页的详细信息，对这些网页进行更改，并将信息传递给父级扩展程序</li>
<li>文档：<a href="https://developer.chrome.com/docs/extensions/develop/concepts/content-scripts?hl=zh-cn">https://developer.chrome.com/docs/extensions/develop/concepts/content-scripts?hl=zh-cn</a></li>
</ol>
</li>
<li><strong>插件的页面</strong><ol>
<li>浏览器的图标以及点击后的效果，例如popup页面：default_popup</li>
<li>options页面：右键插件图标后，下拉框中点击选项后，弹出的页面：options_page</li>
<li>除过上述之外的其他插件页面</li>
<li>注意：所有的插件页面都可以使用浏览器api</li>
</ol>
</li>
</ul>
<h4 id="2-两种options页面的区别：整页、嵌入式页"><a href="#2-两种options页面的区别：整页、嵌入式页" class="headerlink" title="2.两种options页面的区别：整页、嵌入式页"></a>2.两种options页面的区别：整页、嵌入式页</h4><ul>
<li>整页：系统会在新标签页中显示整页options页面。在manifest中的 “options_page” 字段中注册选项 HTML 文件</li>
<li>嵌入式：在当前页面以弹窗形式展示options页面。如需声明嵌入式选项，请在manifest中的 “options_ui” 字段下注册 HTML 文件，并将 “open_in_tab” 键设置为 false。但如果open_in_tab设置为true，效果与整页一致。</li>
</ul>
<h4 id="3-不同script运行的位置："><a href="#3-不同script运行的位置：" class="headerlink" title="3.不同script运行的位置："></a>3.不同script运行的位置：</h4><ul>
<li>service_worker中的script，当启用插件后，运行在整个后台，日志在插件的service_worker检查视图中查看</li>
<li>插入的script，运行在网页环境中，日志在具体页面的控制台中查看<ol>
<li>静态注入的content_scripts，只要打开matches中匹配到的网页即可执行</li>
<li>通过chrome.scripting.executeScript动态注入的，只有在指定动作触发后才执行。例如：通过service_worker监听tab后，动态注入一段脚本</li>
<li>注意：插入的script，如果不设置其运行环境，默认为”ISOLATED”，运行在该插件的隔离环境中，如果设置 world: “MAIN” ，则该脚本运行在Top中</li>
</ol>
</li>
</ul>
<h4 id="4-参考文档："><a href="#4-参考文档：" class="headerlink" title="4.参考文档："></a>4.参考文档：</h4><ul>
<li><a href="https://docs.plasmo.com/">https://docs.plasmo.com/</a></li>
<li><a href="https://developer.chrome.com/docs/extensions/get-started?hl=zh-cn">https://developer.chrome.com/docs/extensions/get-started?hl=zh-cn</a></li>
<li>官网源码：<a href="https://github.com/GoogleChrome/chrome-extensions-samples/tree/main">https://github.com/GoogleChrome/chrome-extensions-samples/tree/main</a> </li>
<li>B站教程源码： <a href="https://github.com/tuntun0609/chrome-extension-study">https://github.com/tuntun0609/chrome-extension-study</a></li>
<li>B站教程文档：<a href="https://www.yuque.com/tuntun-nozomi/gurht0">https://www.yuque.com/tuntun-nozomi/gurht0</a></li>
</ul>
]]></content>
      <categories>
        <category>编程</category>
      </categories>
      <tags>
        <tag>chrome extensions</tag>
      </tags>
  </entry>
  <entry>
    <title>更新cordova时出现的报错及解决方案</title>
    <url>/2018/06/08/cordovaUpdate/</url>
    <content><![CDATA[<h4 id="今天在利用cordova-build安卓apk的时候出现了报错，并且提示更新cordova，于是执行以下命令做更新："><a href="#今天在利用cordova-build安卓apk的时候出现了报错，并且提示更新cordova，于是执行以下命令做更新：" class="headerlink" title="今天在利用cordova build安卓apk的时候出现了报错，并且提示更新cordova，于是执行以下命令做更新："></a>今天在利用cordova build安卓apk的时候出现了报错，并且提示更新cordova，于是执行以下命令做更新：</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm install -g cordova</span><br></pre></td></tr></table></figure>
<p>结果出现了报错，如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm WARN update-linked node_modules\cordova needs updating to 8.0.0 from 6.4.0 but we can&#x27;t, as it&#x27;s a symlink</span><br></pre></td></tr></table></figure>
<span id="more"></span>
<p>在网上查了半天也没找到原因，于是尝试了先去remove再install，结果成功了，如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm remove -g cordova</span><br><span class="line">npm install -g cordova</span><br></pre></td></tr></table></figure>
<p>至于报错的原因，还有待后面研究下…</p>
]]></content>
      <categories>
        <category>编程</category>
      </categories>
      <tags>
        <tag>npm</tag>
      </tags>
  </entry>
  <entry>
    <title>Express Session 认证流程总结</title>
    <url>/2026/07/07/expressSession/</url>
    <content><![CDATA[<p>Session 认证的核心特点是：</p>
<p><strong>登录状态保存在服务端，客户端只保存一个 sessionId。</strong></p>
<p>客户端通过 cookie 保存 <code>sid</code>，服务端通过这个 <code>sid</code> 去 MongoDB 中查找对应的 session 数据。</p>
<span id="more"></span>

<h2 id="1-注册"><a href="#1-注册" class="headerlink" title="1. 注册"></a>1. 注册</h2><p>用户注册时，提交账号和明文密码。</p>
<p>服务端不要直接保存明文密码，而是使用 <code>bcrypt</code> 对密码进行哈希加密：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> hashedPassword = <span class="keyword">await</span> bcrypt.<span class="title function_">hash</span>(password, <span class="number">10</span>);</span><br></pre></td></tr></table></figure>

<p>然后把用户信息和加密后的密码存入 <code>users</code> 集合。</p>
<p>需要注意：</p>
<p><strong>bcrypt 是哈希，不是加密。</strong></p>
<p>更准确的说法是：</p>
<blockquote>
<p>使用 bcrypt 对密码进行哈希处理，然后保存哈希后的密码。</p>
</blockquote>
<p>因为加密一般可以解密，而哈希不能还原成原密码。</p>
<h2 id="2-登录"><a href="#2-登录" class="headerlink" title="2. 登录"></a>2. 登录</h2><p>用户提交账号和明文密码。</p>
<p>服务端先从 <code>users</code> 集合中查询用户，然后使用：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">bcrypt.<span class="title function_">compare</span>(password, user.<span class="property">password</span>);</span><br></pre></td></tr></table></figure>

<p>对比用户提交的明文密码和数据库中保存的哈希密码。</p>
<p>如果校验通过，调用：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">req.<span class="property">session</span>.<span class="title function_">regenerate</span>();</span><br></pre></td></tr></table></figure>

<p>重新生成新的 <code>sessionId</code>，防止 <strong>Session Fixation 攻击</strong>。</p>
<p>然后把必要的用户信息写入 <code>req.session</code>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">req.<span class="property">session</span>.<span class="property">user</span> = &#123;</span><br><span class="line">  <span class="attr">id</span>: user.<span class="property">_id</span>,</span><br><span class="line">  <span class="attr">username</span>: user.<span class="property">username</span>,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>之后 <code>express-session</code> 会配合 <code>connect-mongo</code>，把 session 数据保存到 MongoDB 的 <code>sessions</code> 集合中。</p>
<h2 id="3-后续请求"><a href="#3-后续请求" class="headerlink" title="3. 后续请求"></a>3. 后续请求</h2><p>登录成功后，浏览器会自动在请求中携带 cookie，例如：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">Cookie: connect.sid=xxx</span><br></pre></td></tr></table></figure>

<p>服务端收到请求后，<code>express-session</code> 会：</p>
<ol>
<li>从 cookie 中读取 <code>sid</code></li>
<li>根据 <code>sid</code> 去 MongoDB 的 <code>sessions</code> 集合中查询 session</li>
<li>如果查到，就把 session 数据还原到 <code>req.session</code></li>
<li>后续中间件就可以通过 <code>req.session.user</code> 判断用户是否登录</li>
</ol>
<p>鉴权中间件一般这样写：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">auth</span>(<span class="params">req, res, next</span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (req.<span class="property">session</span>.<span class="property">user</span>) &#123;</span><br><span class="line">    <span class="title function_">next</span>();</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    res.<span class="title function_">status</span>(<span class="number">401</span>).<span class="title function_">json</span>(&#123; <span class="attr">message</span>: <span class="string">&quot;未登录&quot;</span> &#125;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>判断逻辑：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">req.session.user 存在：已登录</span><br><span class="line">req.session.user 不存在：未登录 / session 过期 / cookie 无效</span><br></pre></td></tr></table></figure>

<h2 id="4-登出"><a href="#4-登出" class="headerlink" title="4. 登出"></a>4. 登出</h2><p>登出时，需要销毁服务端 session：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">req.<span class="property">session</span>.<span class="title function_">destroy</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  res.<span class="title function_">clearCookie</span>(<span class="string">&quot;connect.sid&quot;</span>);</span><br><span class="line">  res.<span class="title function_">json</span>(&#123; <span class="attr">message</span>: <span class="string">&quot;退出成功&quot;</span> &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>这里有两个动作：</p>
<ol>
<li><code>req.session.destroy()</code>：删除服务端 session</li>
<li><code>res.clearCookie()</code>：清除客户端 cookie</li>
</ol>
<p>否则客户端可能还保留旧的 <code>sid</code>。</p>
<h2 id="5-过期清理"><a href="#5-过期清理" class="headerlink" title="5. 过期清理"></a>5. 过期清理</h2><p>session 的过期时间通常由 cookie 的 <code>maxAge</code> 和 <code>connect-mongo</code> 的 TTL 机制共同控制。</p>
<p>例如：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">app.<span class="title function_">use</span>(</span><br><span class="line">  <span class="title function_">session</span>(&#123;</span><br><span class="line">    <span class="attr">secret</span>: <span class="string">&quot;your-secret&quot;</span>,</span><br><span class="line">    <span class="attr">store</span>: <span class="title class_">MongoStore</span>.<span class="title function_">create</span>(&#123;</span><br><span class="line">      <span class="attr">mongoUrl</span>: <span class="string">&quot;mongodb://localhost:27017/test&quot;</span>,</span><br><span class="line">    &#125;),</span><br><span class="line">    <span class="attr">cookie</span>: &#123;</span><br><span class="line">      <span class="attr">maxAge</span>: <span class="number">1000</span> * <span class="number">60</span> * <span class="number">60</span>,</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">resave</span>: <span class="literal">false</span>,</span><br><span class="line">    <span class="attr">saveUninitialized</span>: <span class="literal">false</span>,</span><br><span class="line">  &#125;),</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>这里表示 session 有效期为 1 小时。</p>
<p><code>connect-mongo</code> 会根据 session 的过期时间，清理 MongoDB 中过期的 session 数据。</p>
]]></content>
      <categories>
        <category>编程</category>
      </categories>
      <tags>
        <tag>nodejs</tag>
      </tags>
  </entry>
  <entry>
    <title>express JWT 认证流程总结</title>
    <url>/2026/07/07/expressJWT/</url>
    <content><![CDATA[<p>JWT 最大的特点是：无状态。<br>jsonwebtoken 库本身不存储 token，它只负责两件事：</p>
<ol>
<li>签发 token</li>
<li>验证 token</li>
</ol>
<p>token 签发后交给客户端保存，服务端不需要像 session 一样存储登录状态。</p>
<span id="more"></span>

<h2 id="一、JWT-签发过程"><a href="#一、JWT-签发过程" class="headerlink" title="一、JWT 签发过程"></a>一、JWT 签发过程</h2><p>登录成功后，服务端会使用 jsonwebtoken 签发 token。<br>签发时主要做了几件事：</p>
<ol>
<li><p>构造 header<br>里面包含 token 类型和签名算法。</p>
</li>
<li><p>构造 payload<br>里面一般包含用户信息，例如：</p>
</li>
</ol>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  (userId, username, iat, exp);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其中：</p>
<ul>
<li>iat：签发时间</li>
<li>exp：过期时间</li>
</ul>
<ol start="3">
<li><p>使用 header + payload + secret 计算签名 signature</p>
</li>
<li><p>最终拼接成 token：</p>
</li>
</ol>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">header.payload.signature</span><br></pre></td></tr></table></figure>

<p>需要注意：JWT 的 payload 默认不是加密的，只是 Base64URL 编码，所以不要把密码、手机号、身份证号等敏感信息放进去。</p>
<h2 id="二、JWT-验证过程"><a href="#二、JWT-验证过程" class="headerlink" title="二、JWT 验证过程"></a>二、JWT 验证过程</h2><p>客户端请求接口时，一般会把 accessToken 放到请求头里：</p>
<figure class="highlight http"><table><tr><td class="code"><pre><span class="line"><span class="attribute">Authorization</span><span class="punctuation">: </span>Bearer token</span><br></pre></td></tr></table></figure>

<p>服务端认证中间件会做以下事情：</p>
<ol>
<li><p>从 Authorization 中取出 token</p>
</li>
<li><p>拆分 token，得到：</p>
</li>
</ol>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">header</span><br><span class="line">payload</span><br><span class="line">signature</span><br></pre></td></tr></table></figure>

<ol start="3">
<li><p>使用同一个 secret 和相同算法，重新计算一个新的签名</p>
</li>
<li><p>将新签名和 token 中原来的 signature 对比</p>
</li>
<li><p>如果签名一致，再判断 token 是否过期</p>
</li>
<li><p>全部通过后，说明 token 合法</p>
</li>
<li><p>将解码后的用户信息挂载到：</p>
</li>
</ol>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">req.<span class="property">user</span>;</span><br></pre></td></tr></table></figure>

<p>后续接口就可以通过 req.user 获取当前登录用户信息。</p>
<h2 id="三、JWT-的核心理解"><a href="#三、JWT-的核心理解" class="headerlink" title="三、JWT 的核心理解"></a>三、JWT 的核心理解</h2><p>JWT 的本质是：</p>
<p>服务端签发一个带签名的凭证，客户端保存，后续请求带回来，服务端通过 secret 自行验证这个凭证是否可信。</p>
<p>所以全程最关键的是：</p>
<p>签发时的 secret</p>
<p>只要 secret 没泄露，别人就无法伪造合法 token。</p>
<h2 id="四、AccessToken-和-RefreshToken-的常用设计"><a href="#四、AccessToken-和-RefreshToken-的常用设计" class="headerlink" title="四、AccessToken 和 RefreshToken 的常用设计"></a>四、AccessToken 和 RefreshToken 的常用设计</h2><p>实际项目中常用的是双令牌机制：</p>
<h3 id="1-accessToken"><a href="#1-accessToken" class="headerlink" title="1. accessToken"></a>1. accessToken</h3><p>特点：<br>短时间有效，无状态，自校验</p>
<p>常见有效期：<br>15分钟</p>
<p>使用方式：<br>前端放在请求头 Authorization 中</p>
<p>它主要用于访问普通业务接口。</p>
<h3 id="2-refreshToken"><a href="#2-refreshToken" class="headerlink" title="2. refreshToken"></a>2. refreshToken</h3><p>特点：<br>长时间有效，服务端需要存储</p>
<p>常见有效期：<br>7天</p>
<p>使用方式：<br>存 httpOnly cookie，同时服务端数据库或 Redis 中也保存一份</p>
<p>它主要用于在 accessToken 过期后，重新换取新的 accessToken。</p>
<h2 id="五、双令牌认证系统接口流程"><a href="#五、双令牌认证系统接口流程" class="headerlink" title="五、双令牌认证系统接口流程"></a>五、双令牌认证系统接口流程</h2><p>以 Express + MongoDB + JWT 为例，可以设计四个核心接口：</p>
<h3 id="1-注册接口"><a href="#1-注册接口" class="headerlink" title="1. 注册接口"></a>1. 注册接口</h3><p>流程：<br>接收用户名和密码<br>bcrypt 加密密码<br>保存用户信息到 users 集合</p>
<p>重点：<br>数据库中不能保存明文密码</p>
<h3 id="2-登录接口"><a href="#2-登录接口" class="headerlink" title="2. 登录接口"></a>2. 登录接口</h3><p>流程：<br>校验用户名和密码<br>bcrypt 对比密码<br>签发 accessToken<br>签发 refreshToken<br>refreshToken 存入数据库<br>refreshToken 写入 httpOnly cookie<br>accessToken 放响应体返回</p>
<p>重点：<br>accessToken 给前端使用<br>refreshToken 放 cookie，并且服务端存一份</p>
<h3 id="3-刷新令牌接口"><a href="#3-刷新令牌接口" class="headerlink" title="3. 刷新令牌接口"></a>3. 刷新令牌接口</h3><p>流程：<br>从 cookie 中读取 refreshToken<br>验证 refreshToken 签名和过期时间<br>查询数据库中是否存在这个 refreshToken<br>如果有效，签发新的 accessToken<br>同时轮换新的 refreshToken<br>删除旧 refreshToken<br>保存新 refreshToken<br>重新写入 cookie</p>
<p>重点：<br>refreshToken 每次使用后都要换新的，这叫 refreshToken 轮换<br>这样可以降低 refreshToken 被盗后的风险。</p>
<h3 id="4-登出接口"><a href="#4-登出接口" class="headerlink" title="4. 登出接口"></a>4. 登出接口</h3><p>流程：<br>从 cookie 中读取 refreshToken<br>删除数据库中的 refreshToken<br>清除 cookie</p>
<p>重点：<br>accessToken 本身无状态，无法直接删除<br>所以登出主要是让 refreshToken 失效<br>accessToken 因为有效期很短，一般等它自然过期即可。</p>
<h2 id="六、认证中间件流程"><a href="#六、认证中间件流程" class="headerlink" title="六、认证中间件流程"></a>六、认证中间件流程</h2><p>认证中间件主要负责保护需要登录后才能访问的接口。</p>
<p>流程：<br>从 Authorization 请求头中读取 Bearer token<br>验证 accessToken 签名<br>判断是否过期<br>解析 payload<br>把用户信息挂载到 req.user<br>放行请求</p>
<p>示例理解：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">req.<span class="property">user</span> = &#123;</span><br><span class="line">  userId,</span><br><span class="line">  username,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>后面的接口就可以通过 req.user.userId 判断当前是谁在操作。</p>
<h2 id="七、安全要点"><a href="#七、安全要点" class="headerlink" title="七、安全要点"></a>七、安全要点</h2><p>需要注意几个关键点：</p>
<ol>
<li><p>accessToken 有效期要短<br>一般设置为 15 分钟左右。</p>
</li>
<li><p>refreshToken 有效期可以长一些<br>比如 7 天、14 天。</p>
</li>
<li><p>refreshToken 要存服务端<br>可以存 MongoDB，也可以存 Redis。</p>
</li>
<li><p>refreshToken 要轮换<br>每次刷新令牌后，旧 refreshToken 作废，生成新的 refreshToken。</p>
</li>
<li><p>accessToken 和 refreshToken 的 secret 必须分开</p>
</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ACCESS_TOKEN_SECRET=xxx</span><br><span class="line">REFRESH_TOKEN_SECRET=yyy</span><br></pre></td></tr></table></figure>

<p>payload 中不要放敏感信息<br>JWT payload 可以被前端解析出来，所以只放必要信息，例如：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  (userId, username);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="八、一句话总结"><a href="#八、一句话总结" class="headerlink" title="八、一句话总结"></a>八、一句话总结</h2><p>JWT 是一种无状态认证方案，服务端通过 secret 对 token 进行签名和验证；实际项目中通常使用 accessToken + refreshToken 双令牌机制，accessToken 负责访问接口，refreshToken 负责续期，而串联整个认证流程的核心是 userId。</p>
]]></content>
      <categories>
        <category>编程</category>
      </categories>
      <tags>
        <tag>nodejs</tag>
      </tags>
  </entry>
  <entry>
    <title>分析ios闪退日志的简易方法</title>
    <url>/2019/05/08/iosCrashAnalyse/</url>
    <content><![CDATA[<ol>
<li>首先在打包机上找到dSYM文件，每次发布都会被覆盖为最新的，具体目录为jenkins对应的pipeline下platform&#x2F;ios&#x2F;build&#x2F;device&#x2F;，拷贝至桌面方便操作；</li>
<li>将设备链接至mac，在xcode的window-&gt;devices and simulators-&gt;下点击view device logs，在this device下等待所有的日志加载好后点击type排序，在对应的app里找到闪退时间一致的闪退日志，查看日志；<span id="more"></span></li>
<li>对比闪退日志和dSYM日志的uuid，确认dSYM文件，在桌面执行命令<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">dwarfdump --uuid SogalNew.app.dSYM</span><br></pre></td></tr></table></figure>
解析dSYM文件的uuid，输出结果如下<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">UUID: 89E89BFC-43CE-3BC6-8D16-07C24394B0DB (armv7)</span><br><span class="line">dSYMs/SogalNew.app.dSYM/Contents/Resources/DWARF/SogalNew</span><br><span class="line">UUID: 101C2198-E694-3D3A-9BC6-F7A30D4451EF (arm64)</span><br><span class="line">dSYMs/SogalNew.app.dSYM/Contents/Resources/DWARF/SogalNew</span><br></pre></td></tr></table></figure>
在闪退日志里找到如下一行，对比&lt;&gt;里的uuid，在确保都为arm64时如果一致代表dSYM文件可以用<img src="/2019/05/08/iosCrashAnalyse/uuid.png" class="" title="uuid"></li>
<li>在桌面执行如下命令，开始解析闪退日志：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/Applications/Xcode.app/Contents/Developer/usr/bin/atos -o SogalNew.app.dSYM/Contents/Resources/DWARF/SogalNew -l 0x100084000 -arch arm64</span><br></pre></td></tr></table></figure>
其中0x100084000与<img src="/2019/05/08/iosCrashAnalyse/uuid1.png" class="" title="uuid1">
及<img src="/2019/05/08/iosCrashAnalyse/uuid2.png" class="" title="uuid2">
一致</li>
<li>接下来输入闪退日志里前几句<img src="/2019/05/08/iosCrashAnalyse/uuid3.png" class="" title="uuid3">进行解析即可看到大致报错位置。</li>
</ol>
<p>注：SogalNew指应用名称</p>
]]></content>
      <categories>
        <category>编程</category>
      </categories>
      <tags>
        <tag>js</tag>
      </tags>
  </entry>
  <entry>
    <title>js promise简例及一些注意点</title>
    <url>/2018/05/11/jsPromise/</url>
    <content><![CDATA[<h4 id="无论promise中的执行结果是什么，它总会给你返回一个状态，成功或者失败"><a href="#无论promise中的执行结果是什么，它总会给你返回一个状态，成功或者失败" class="headerlink" title="无论promise中的执行结果是什么，它总会给你返回一个状态，成功或者失败"></a>无论promise中的执行结果是什么，它总会给你返回一个状态，成功或者失败</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//封装promise</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">getURL</span>(<span class="params">URL</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Promise</span>(<span class="keyword">function</span> (<span class="params">resolve, reject</span>) &#123;</span><br><span class="line">        <span class="keyword">var</span> req = <span class="keyword">new</span> <span class="title class_">XMLHttpRequest</span>();</span><br><span class="line">        req.<span class="title function_">open</span>(<span class="string">&#x27;GET&#x27;</span>, <span class="variable constant_">URL</span>, <span class="literal">true</span>);</span><br><span class="line">        req.<span class="property">onload</span> = <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (req.<span class="property">status</span> === <span class="number">200</span>) &#123;</span><br><span class="line">                <span class="title function_">resolve</span>(req.<span class="property">responseText</span>);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="title function_">reject</span>(<span class="keyword">new</span> <span class="title class_">Error</span>(req.<span class="property">statusText</span>));</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        req.<span class="property">onerror</span> = <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">            <span class="title function_">reject</span>(<span class="keyword">new</span> <span class="title class_">Error</span>(req.<span class="property">statusText</span>));</span><br><span class="line">        &#125;;</span><br><span class="line">        req.<span class="title function_">send</span>();</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//执行</span></span><br><span class="line"><span class="keyword">var</span> <span class="variable constant_">URL</span> = <span class="string">&quot;http:***&quot;</span>;</span><br><span class="line"><span class="title function_">getURL</span>(<span class="variable constant_">URL</span>).<span class="title function_">then</span>(<span class="keyword">function</span>(<span class="params">value</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(value);</span><br><span class="line">&#125;).<span class="title function_">catch</span>(<span class="keyword">function</span>(<span class="params">error</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">error</span>(error);</span><br><span class="line">&#125;);</span><br><span class="line"><span class="comment">//也可以这样写</span></span><br><span class="line"><span class="comment">//getURL(URL).then(function(value) &#123;</span></span><br><span class="line"><span class="comment">//    console.log(value);</span></span><br><span class="line"><span class="comment">//&#125;, function(error)&#123;</span></span><br><span class="line"><span class="comment">//    console.error(error);</span></span><br><span class="line"><span class="comment">//&#125;)</span></span><br></pre></td></tr></table></figure>
<span id="more"></span>
<h4 id="在angularjs中可以通过-q实现异步操作："><a href="#在angularjs中可以通过-q实现异步操作：" class="headerlink" title="在angularjs中可以通过$q实现异步操作："></a>在angularjs中可以通过$q实现异步操作：</h4><p>通过调用  $q.defer() 可以构建一个新的 deffered 实例,deffered 对象用来将 Promise 实例与 标记任务状态(执行成功还是不成功)的 API 相关联，<br>deffered 对象的方法</p>
<blockquote>
<ul>
<li>resolve(value) ——成功，如果 value 是一个通过 $q.reject 构造的拒绝对象(rejection) , 该promise 将被拒绝。</li>
<li>reject(reason) ——失败，这相当于通过 $q.reject构造的拒绝对象(rejection)作为参数传递给 resolve。</li>
<li>notify(value)  ——在 promise 执行的过程中提供状态更新。 这在 promise 被解决或拒绝之前可能会被多次调用。</li>
</ul>
</blockquote>
<p>例子如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//封装promise</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">asyncGreet</span>(<span class="params">name</span>) &#123;</span><br><span class="line">    <span class="keyword">var</span> deferred = $q.<span class="title function_">defer</span>();</span><br><span class="line">    <span class="built_in">setTimeout</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">        deferred.<span class="title function_">notify</span>(<span class="string">&#x27;即将问候 &#x27;</span> + name + <span class="string">&#x27;.&#x27;</span>);</span><br><span class="line">        <span class="keyword">if</span> (<span class="title function_">okToGreet</span>(name)) &#123;</span><br><span class="line">            deferred.<span class="title function_">resolve</span>(<span class="string">&#x27;你好, &#x27;</span> + name + <span class="string">&#x27;!&#x27;</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            deferred.<span class="title function_">reject</span>(<span class="string">&#x27;拒绝问候 &#x27;</span> + name + <span class="string">&#x27; .&#x27;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;, <span class="number">1000</span>);</span><br><span class="line">    <span class="keyword">return</span> deferred.<span class="property">promise</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//执行</span></span><br><span class="line"><span class="keyword">var</span> promise = <span class="title function_">asyncGreet</span>(<span class="string">&#x27;***&#x27;</span>);</span><br><span class="line">promise.<span class="title function_">then</span>(<span class="keyword">function</span> (<span class="params">res</span>) &#123;</span><br><span class="line"></span><br><span class="line">&#125;, <span class="keyword">function</span> (<span class="params">error</span>) &#123;</span><br><span class="line"></span><br><span class="line">&#125;, <span class="keyword">function</span> (<span class="params">update</span>) &#123;</span><br><span class="line"></span><br><span class="line">&#125;);  </span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>编程</category>
      </categories>
      <tags>
        <tag>js</tag>
      </tags>
  </entry>
  <entry>
    <title>js中的Map和Set</title>
    <url>/2018/07/25/mapset/</url>
    <content><![CDATA[<h2 id="1-Map"><a href="#1-Map" class="headerlink" title="1.Map"></a>1.Map</h2><h4 id="Map是一组键值对的结构，具有极快的查找速度，如下："><a href="#Map是一组键值对的结构，具有极快的查找速度，如下：" class="headerlink" title="Map是一组键值对的结构，具有极快的查找速度，如下："></a>Map是一组键值对的结构，具有极快的查找速度，如下：</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> m = <span class="keyword">new</span> <span class="title class_">Map</span>(); <span class="comment">// 创建一个空Map</span></span><br><span class="line"><span class="comment">//也可以像这样创建：var m = new Map([[&#x27;Michael&#x27;, 95], [&#x27;Bob&#x27;, 75], [&#x27;Tracy&#x27;, 85]]);</span></span><br><span class="line">m.<span class="title function_">set</span>(<span class="string">&#x27;leo&#x27;</span>, <span class="number">25</span>); <span class="comment">// 添加新的key-value</span></span><br><span class="line">m.<span class="title function_">set</span>(<span class="string">&#x27;Bob&#x27;</span>, <span class="number">30</span>);</span><br><span class="line">m.<span class="title function_">set</span>(<span class="number">10</span>, <span class="string">&quot;john&quot;</span>);</span><br><span class="line">m.<span class="title function_">has</span>(<span class="string">&#x27;leo&#x27;</span>); <span class="comment">// 是否存在key &#x27;Adam&#x27;: true</span></span><br><span class="line">m.<span class="title function_">get</span>(<span class="string">&#x27;leo&#x27;</span>); <span class="comment">// 25</span></span><br><span class="line">m.<span class="title function_">delete</span>(<span class="string">&#x27;leo&#x27;</span>); <span class="comment">// 删除key &#x27;leo&#x27;</span></span><br><span class="line">m.<span class="title function_">get</span>(<span class="string">&#x27;leo&#x27;</span>); <span class="comment">// undefined</span></span><br><span class="line">m.<span class="property">size</span>;<span class="comment">// 获取map的长度：2</span></span><br></pre></td></tr></table></figure>
<span id="more"></span>
<h4 id="由于一个key只能对应一个value，所以，多次对一个key放入value，后面的value会把之前的覆盖："><a href="#由于一个key只能对应一个value，所以，多次对一个key放入value，后面的value会把之前的覆盖：" class="headerlink" title="由于一个key只能对应一个value，所以，多次对一个key放入value，后面的value会把之前的覆盖："></a>由于一个key只能对应一个value，所以，多次对一个key放入value，后面的value会把之前的覆盖：</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">m.<span class="title function_">set</span>(<span class="string">&#x27;Bob&#x27;</span>, <span class="number">300</span>);</span><br><span class="line">m.<span class="title function_">get</span>(<span class="string">&#x27;Bob&#x27;</span>); <span class="comment">// 300</span></span><br></pre></td></tr></table></figure>
<h4 id="在Map中，key值可以是任何基本类型-String-Number-Boolean-undefined-NaN…-，或者对象-Map-Set-Object-Function-Symbol-null…"><a href="#在Map中，key值可以是任何基本类型-String-Number-Boolean-undefined-NaN…-，或者对象-Map-Set-Object-Function-Symbol-null…" class="headerlink" title="在Map中，key值可以是任何基本类型(String, Number, Boolean, undefined, NaN….)，或者对象(Map, Set, Object, Function , Symbol , null…)"></a>在Map中，key值可以是任何基本类型(String, Number, Boolean, undefined, NaN….)，或者对象(Map, Set, Object, Function , Symbol , null…)</h4><h2 id="2-Set"><a href="#2-Set" class="headerlink" title="2.Set"></a>2.Set</h2><h4 id="Set和Map类似，也是一组key的集合，但不存储value。由于key不能重复，所以，在Set中，没有重复的key"><a href="#Set和Map类似，也是一组key的集合，但不存储value。由于key不能重复，所以，在Set中，没有重复的key" class="headerlink" title="Set和Map类似，也是一组key的集合，但不存储value。由于key不能重复，所以，在Set中，没有重复的key"></a>Set和Map类似，也是一组key的集合，但不存储value。由于key不能重复，所以，在Set中，没有重复的key</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> s = <span class="keyword">new</span> <span class="title class_">Set</span>(); <span class="comment">// 创建一个空Set</span></span><br><span class="line"><span class="comment">//也可以像这样创建：var s2 = new Set([1, 2, 3]);</span></span><br><span class="line"><span class="keyword">var</span> s = <span class="keyword">new</span> <span class="title class_">Set</span>([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="string">&#x27;3&#x27;</span>]);<span class="comment">//重复元素在Set中自动被过滤：&#123;1, 2, 3, &quot;3&quot;&#125;</span></span><br><span class="line"><span class="keyword">var</span> s = <span class="keyword">new</span> <span class="title class_">Set</span>();<span class="comment">//&#123;&#125;</span></span><br><span class="line">s.<span class="title function_">add</span>(<span class="number">10</span>);<span class="comment">//添加元素</span></span><br><span class="line">s.<span class="title function_">add</span>(<span class="number">100</span>);</span><br><span class="line">s.<span class="title function_">add</span>(<span class="string">&quot;10&quot;</span>);<span class="comment">//&#123;10, 100, &quot;10&quot;&#125;</span></span><br><span class="line">s.<span class="title function_">delete</span>(<span class="number">10</span>);<span class="comment">//删除：&#123;100, &quot;10&quot;&#125;</span></span><br><span class="line">s.<span class="property">size</span>;<span class="comment">//获取长度：2</span></span><br></pre></td></tr></table></figure>
<h4 id="在Set中，key值可以是任何基本类型-String-Number-Boolean-undefined-NaN…-，或者对象-Map-Set-Object-Function-Symbol-null…"><a href="#在Set中，key值可以是任何基本类型-String-Number-Boolean-undefined-NaN…-，或者对象-Map-Set-Object-Function-Symbol-null…" class="headerlink" title="在Set中，key值可以是任何基本类型(String, Number, Boolean, undefined, NaN….)，或者对象(Map, Set, Object, Function , Symbol , null…)"></a>在Set中，key值可以是任何基本类型(String, Number, Boolean, undefined, NaN….)，或者对象(Map, Set, Object, Function , Symbol , null…)</h4>]]></content>
      <categories>
        <category>编程</category>
      </categories>
      <tags>
        <tag>js</tag>
      </tags>
  </entry>
  <entry>
    <title>JSBridge</title>
    <url>/2026/07/24/jsbridge/</url>
    <content><![CDATA[<h2 id="JSBridge是什么"><a href="#JSBridge是什么" class="headerlink" title="JSBridge是什么"></a>JSBridge是什么</h2><p>JSBridge是一种让<strong>Web页面里的JavaScript</strong>与<strong>原生应用代码</strong>相互通信的机制。</p>
<p>常见场景：</p>
<ul>
<li>App中嵌入H5页面</li>
<li>JavaScript调用原生能力</li>
<li>原生代码主动通知H5页面</li>
</ul>
<p>例如，H5页面本身不能直接访问手机通讯录、相机、定位、支付等能力，就需要通过JSBridge让原生端代为执行。</p>
<span id="more"></span>

<hr>
<h2 id="基本通信过程"><a href="#基本通信过程" class="headerlink" title="基本通信过程"></a>基本通信过程</h2><p>以H5调用相机为例：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">H5 JavaScript</span><br><span class="line">    ↓ 调用Bridge方法</span><br><span class="line">JSBridge</span><br><span class="line">    ↓ 传递方法名和参数</span><br><span class="line">Android/iOS原生代码</span><br><span class="line">    ↓ 调用相机</span><br><span class="line">执行完成</span><br><span class="line">    ↓ 返回结果</span><br><span class="line">JSBridge</span><br><span class="line">    ↓</span><br><span class="line">JavaScript回调或Promise</span><br></pre></td></tr></table></figure>

<p>H5侧可能这样调用：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">window</span>.<span class="property">JSBridge</span>.<span class="title function_">invoke</span>(<span class="string">&#x27;openCamera&#x27;</span>, &#123;</span><br><span class="line">  <span class="attr">quality</span>: <span class="number">0.8</span></span><br><span class="line">&#125;).<span class="title function_">then</span>(<span class="function"><span class="params">result</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(result.<span class="property">imageUrl</span>);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>原生端接收到：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;method&quot;</span><span class="punctuation">:</span> <span class="string">&quot;openCamera&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;params&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;quality&quot;</span><span class="punctuation">:</span> <span class="number">0.8</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>执行完成后，再把结果返回给JavaScript。</p>
<hr>
<h2 id="JSBridge能做什么"><a href="#JSBridge能做什么" class="headerlink" title="JSBridge能做什么"></a>JSBridge能做什么</h2><p>常见能力包括：</p>
<ul>
<li>获取设备信息</li>
<li>获取定位</li>
<li>调用相机和相册</li>
<li>扫码</li>
<li>文件上传和下载</li>
<li>分享</li>
<li>支付</li>
<li>设置导航栏</li>
<li>关闭当前WebView</li>
<li>获取登录用户信息</li>
<li>原生页面跳转</li>
<li>原生向H5推送事件</li>
</ul>
<hr>
<h2 id="两个通信方向"><a href="#两个通信方向" class="headerlink" title="两个通信方向"></a>两个通信方向</h2><h3 id="JavaScript调用原生"><a href="#JavaScript调用原生" class="headerlink" title="JavaScript调用原生"></a>JavaScript调用原生</h3><p>例如H5调用扫码：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">bridge.<span class="title function_">invoke</span>(<span class="string">&#x27;scanQRCode&#x27;</span>, &#123;</span><br><span class="line">  <span class="attr">type</span>: <span class="string">&#x27;qr&#x27;</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>原生端根据<code>scanQRCode</code>找到对应处理方法并执行。</p>
<h3 id="原生调用JavaScript"><a href="#原生调用JavaScript" class="headerlink" title="原生调用JavaScript"></a>原生调用JavaScript</h3><p>原生端可以直接执行WebView中的JavaScript：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">window</span>.<span class="title function_">onNetworkChange</span>(&#123;</span><br><span class="line">  <span class="attr">type</span>: <span class="string">&#x27;wifi&#x27;</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>H5提前注册方法：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">window</span>.<span class="property">onNetworkChange</span> = <span class="keyword">function</span> (<span class="params">data</span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;网络发生变化&#x27;</span>, data);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>实际项目中通常会封装成事件机制：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">bridge.<span class="title function_">on</span>(<span class="string">&#x27;networkChange&#x27;</span>, <span class="function"><span class="params">data</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(data);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="常见实现方式"><a href="#常见实现方式" class="headerlink" title="常见实现方式"></a>常见实现方式</h2><h3 id="Android"><a href="#Android" class="headerlink" title="Android"></a>Android</h3><p>Android WebView常见方式：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">webView.addJavascriptInterface(</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">NativeBridge</span>(),</span><br><span class="line">    <span class="string">&quot;NativeBridge&quot;</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>JavaScript调用：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">window</span>.<span class="property">NativeBridge</span>.<span class="title function_">openCamera</span>();</span><br></pre></td></tr></table></figure>

<p>也可以通过拦截URL、<code>postMessage</code>等方式通信。</p>
<h3 id="iOS"><a href="#iOS" class="headerlink" title="iOS"></a>iOS</h3><p>iOS的WKWebView常使用：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">window</span>.<span class="property">webkit</span>.<span class="property">messageHandlers</span>.<span class="property">nativeBridge</span>.<span class="title function_">postMessage</span>(&#123;</span><br><span class="line">  <span class="attr">method</span>: <span class="string">&#x27;openCamera&#x27;</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>iOS原生端监听<code>nativeBridge</code>并处理消息。</p>
<h3 id="React-Native"><a href="#React-Native" class="headerlink" title="React Native"></a>React Native</h3><p>React Native的WebView通常使用：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">window</span>.<span class="property">ReactNativeWebView</span>.<span class="title function_">postMessage</span>(</span><br><span class="line">  <span class="title class_">JSON</span>.<span class="title function_">stringify</span>(&#123;</span><br><span class="line">    <span class="attr">method</span>: <span class="string">&#x27;openCamera&#x27;</span></span><br><span class="line">  &#125;)</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>React Native侧通过<code>onMessage</code>接收。</p>
<hr>
<h2 id="实际项目中的统一封装"><a href="#实际项目中的统一封装" class="headerlink" title="实际项目中的统一封装"></a>实际项目中的统一封装</h2><p>为了屏蔽Android、iOS之间的差异，前端一般不会直接调用底层API，而是统一封装：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">JSBridge</span> &#123;</span><br><span class="line">  <span class="title function_">constructor</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">callbacks</span> = <span class="keyword">new</span> <span class="title class_">Map</span>();</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">callbackId</span> = <span class="number">0</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">invoke</span>(<span class="params">method, params = &#123;&#125;</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">const</span> callbackId = <span class="string">`callback_<span class="subst">$&#123;++<span class="variable language_">this</span>.callbackId&#125;</span>`</span>;</span><br><span class="line"></span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">callbacks</span>.<span class="title function_">set</span>(callbackId, &#123;</span><br><span class="line">        resolve,</span><br><span class="line">        reject</span><br><span class="line">      &#125;);</span><br><span class="line"></span><br><span class="line">      <span class="keyword">const</span> message = &#123;</span><br><span class="line">        method,</span><br><span class="line">        params,</span><br><span class="line">        callbackId</span><br><span class="line">      &#125;;</span><br><span class="line"></span><br><span class="line">      <span class="variable language_">this</span>.<span class="title function_">send</span>(message);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">send</span>(<span class="params">message</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> data = <span class="title class_">JSON</span>.<span class="title function_">stringify</span>(message);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="variable language_">window</span>.<span class="property">webkit</span>?.<span class="property">messageHandlers</span>?.<span class="property">nativeBridge</span>) &#123;</span><br><span class="line">      <span class="variable language_">window</span>.<span class="property">webkit</span>.<span class="property">messageHandlers</span>.<span class="property">nativeBridge</span>.<span class="title function_">postMessage</span>(message);</span><br><span class="line">      <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="variable language_">window</span>.<span class="property">ReactNativeWebView</span>) &#123;</span><br><span class="line">      <span class="variable language_">window</span>.<span class="property">ReactNativeWebView</span>.<span class="title function_">postMessage</span>(data);</span><br><span class="line">      <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="variable language_">window</span>.<span class="property">NativeBridge</span>?.<span class="property">postMessage</span>) &#123;</span><br><span class="line">      <span class="variable language_">window</span>.<span class="property">NativeBridge</span>.<span class="title function_">postMessage</span>(data);</span><br><span class="line">      <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&#x27;当前环境不支持JSBridge&#x27;</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">handleCallback</span>(<span class="params">callbackId, result, error</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> callback = <span class="variable language_">this</span>.<span class="property">callbacks</span>.<span class="title function_">get</span>(callbackId);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!callback) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (error) &#123;</span><br><span class="line">      callback.<span class="title function_">reject</span>(error);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      callback.<span class="title function_">resolve</span>(result);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">callbacks</span>.<span class="title function_">delete</span>(callbackId);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>调用：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> bridge = <span class="keyword">new</span> <span class="title class_">JSBridge</span>();</span><br><span class="line"></span><br><span class="line">bridge.<span class="title function_">invoke</span>(<span class="string">&#x27;getLocation&#x27;</span>).<span class="title function_">then</span>(<span class="function"><span class="params">location</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(location);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>原生执行完成后调用：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">bridge.<span class="title function_">handleCallback</span>(</span><br><span class="line">  <span class="string">&#x27;callback_1&#x27;</span>,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">latitude</span>: <span class="number">34.34</span>,</span><br><span class="line">    <span class="attr">longitude</span>: <span class="number">108.94</span></span><br><span class="line">  &#125;</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="为什么需要callbackId"><a href="#为什么需要callbackId" class="headerlink" title="为什么需要callbackId"></a>为什么需要callbackId</h2><p>调用原生能力通常是异步的，而且可能同时发起多个请求：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">bridge.<span class="title function_">invoke</span>(<span class="string">&#x27;getLocation&#x27;</span>);</span><br><span class="line">bridge.<span class="title function_">invoke</span>(<span class="string">&#x27;openCamera&#x27;</span>);</span><br><span class="line">bridge.<span class="title function_">invoke</span>(<span class="string">&#x27;getDeviceInfo&#x27;</span>);</span><br></pre></td></tr></table></figure>

<p>因此每次调用都生成唯一的<code>callbackId</code>：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;method&quot;</span><span class="punctuation">:</span> <span class="string">&quot;getLocation&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;params&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span><span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;callbackId&quot;</span><span class="punctuation">:</span> <span class="string">&quot;callback_1&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>原生返回时携带相同的<code>callbackId</code>：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;callbackId&quot;</span><span class="punctuation">:</span> <span class="string">&quot;callback_1&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;result&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;latitude&quot;</span><span class="punctuation">:</span> <span class="number">34.34</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>JSBridge根据它找到对应的Promise或回调函数。</p>
<hr>
<h2 id="JSBridge的本质"><a href="#JSBridge的本质" class="headerlink" title="JSBridge的本质"></a>JSBridge的本质</h2><p>JSBridge本质上就是一个<strong>消息协议和调用映射机制</strong>：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">方法名 + 参数 + 请求ID</span><br></pre></td></tr></table></figure>

<p>请求格式：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;method&quot;</span><span class="punctuation">:</span> <span class="string">&quot;openCamera&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;params&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span><span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;callbackId&quot;</span><span class="punctuation">:</span> <span class="string">&quot;callback_1&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>响应格式：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;callbackId&quot;</span><span class="punctuation">:</span> <span class="string">&quot;callback_1&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;success&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;data&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span><span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>所以JSBridge并不是某个固定框架，而是一套Web与原生之间约定好的通信方案。</p>
<hr>
<h2 id="实际开发需要注意"><a href="#实际开发需要注意" class="headerlink" title="实际开发需要注意"></a>实际开发需要注意</h2><h3 id="安全控制"><a href="#安全控制" class="headerlink" title="安全控制"></a>安全控制</h3><p>不能允许H5随意调用所有原生方法，应设置白名单：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> allowedMethods = [</span><br><span class="line">  <span class="string">&#x27;getDeviceInfo&#x27;</span>,</span><br><span class="line">  <span class="string">&#x27;openCamera&#x27;</span>,</span><br><span class="line">  <span class="string">&#x27;scanQRCode&#x27;</span></span><br><span class="line">];</span><br></pre></td></tr></table></figure>

<p>还要校验当前H5页面的域名，防止恶意网页调用支付、登录信息等敏感能力。</p>
<h3 id="参数校验"><a href="#参数校验" class="headerlink" title="参数校验"></a>参数校验</h3><p>原生端不能完全信任前端参数：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;method&quot;</span><span class="punctuation">:</span> <span class="string">&quot;openCamera&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;params&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;quality&quot;</span><span class="punctuation">:</span> <span class="string">&quot;错误类型&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>双方都需要约定参数类型和必填字段。</p>
<h3 id="超时处理"><a href="#超时处理" class="headerlink" title="超时处理"></a>超时处理</h3><p>原生端可能没有返回结果，需要设置超时：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="title class_">Promise</span>.<span class="title function_">race</span>([</span><br><span class="line">  bridge.<span class="title function_">invoke</span>(<span class="string">&#x27;getLocation&#x27;</span>),</span><br><span class="line">  <span class="title function_">timeout</span>(<span class="number">5000</span>)</span><br><span class="line">]);</span><br></pre></td></tr></table></figure>

<h3 id="版本兼容"><a href="#版本兼容" class="headerlink" title="版本兼容"></a>版本兼容</h3><p>新版本App可能增加Bridge方法，而旧版本没有，因此通常需要能力判断：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">bridge.<span class="title function_">supports</span>(<span class="string">&#x27;openCamera&#x27;</span>);</span><br></pre></td></tr></table></figure>

<p>或者维护版本号：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">bridge.<span class="title function_">getVersion</span>();</span><br></pre></td></tr></table></figure>

<h3 id="页面销毁时清理"><a href="#页面销毁时清理" class="headerlink" title="页面销毁时清理"></a>页面销毁时清理</h3><p>页面关闭后，应清理未完成的回调和事件监听，避免内存泄漏。</p>
<hr>
<h2 id="和普通Ajax的区别"><a href="#和普通Ajax的区别" class="headerlink" title="和普通Ajax的区别"></a>和普通Ajax的区别</h2><table>
<thead>
<tr>
<th>对比项</th>
<th>JSBridge</th>
<th>Ajax&#x2F;Fetch</th>
</tr>
</thead>
<tbody><tr>
<td>通信对象</td>
<td>JavaScript与原生App</td>
<td>浏览器与服务器</td>
</tr>
<tr>
<td>通信范围</td>
<td>当前WebView和App</td>
<td>HTTP网络</td>
</tr>
<tr>
<td>主要用途</td>
<td>调用设备和App能力</td>
<td>请求后端数据</td>
</tr>
<tr>
<td>是否一定联网</td>
<td>不一定</td>
<td>通常需要</td>
</tr>
<tr>
<td>常见能力</td>
<td>相机、定位、支付、页面跳转</td>
<td>查询、提交业务数据</td>
</tr>
</tbody></table>
<hr>
]]></content>
  </entry>
  <entry>
    <title>线上性能监控方案</title>
    <url>/2026/07/07/performanceMonitor/</url>
    <content><![CDATA[<h2 id="一、监控目标"><a href="#一、监控目标" class="headerlink" title="一、监控目标"></a>一、监控目标</h2><p>前端性能监控的核心目的不是单纯看页面快不快，而是：</p>
<ol>
<li>发现页面加载慢、白屏、卡顿等问题；</li>
<li>定位是网络、资源、接口、渲染还是 JS 执行导致；</li>
<li>量化用户真实体验；</li>
<li>为性能优化提供数据依据；</li>
<li>线上出现性能劣化时能及时报警。</li>
</ol>
<span id="more"></span>

<h2 id="二、核心监控指标"><a href="#二、核心监控指标" class="headerlink" title="二、核心监控指标"></a>二、核心监控指标</h2><h3 id="1-页面加载性能指标"><a href="#1-页面加载性能指标" class="headerlink" title="1. 页面加载性能指标"></a>1. 页面加载性能指标</h3><p>常见指标包括：</p>
<table>
<thead>
<tr>
<th>指标</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td>FP</td>
<td>First Paint，首次绘制</td>
</tr>
<tr>
<td>FCP</td>
<td>First Contentful Paint，首次内容绘制</td>
</tr>
<tr>
<td>LCP</td>
<td>Largest Contentful Paint，最大内容绘制</td>
</tr>
<tr>
<td>DOMContentLoaded</td>
<td>DOM 解析完成时间</td>
</tr>
<tr>
<td>Load</td>
<td>页面资源完全加载时间</td>
</tr>
<tr>
<td>TTFB</td>
<td>首字节时间，反映服务端响应速度</td>
</tr>
<tr>
<td>DNS</td>
<td>DNS 查询耗时</td>
</tr>
<tr>
<td>TCP</td>
<td>TCP 连接耗时</td>
</tr>
<tr>
<td>SSL</td>
<td>HTTPS 握手耗时</td>
</tr>
<tr>
<td>Resource Timing</td>
<td>JS、CSS、图片等资源加载耗时</td>
</tr>
</tbody></table>
<p>面试里重点说：</p>
<blockquote>
<p>我们会重点关注 FCP、LCP、TTFB、资源加载耗时和接口耗时，这些指标可以比较直观地反映页面首屏体验和网络链路问题。</p>
</blockquote>
<h3 id="2-用户体验指标"><a href="#2-用户体验指标" class="headerlink" title="2. 用户体验指标"></a>2. 用户体验指标</h3><p>除了加载速度，还要关注用户交互体验。</p>
<table>
<thead>
<tr>
<th>指标</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td>CLS</td>
<td>页面布局偏移</td>
</tr>
<tr>
<td>INP</td>
<td>用户交互响应延迟</td>
</tr>
<tr>
<td>Long Task</td>
<td>超过 50ms 的长任务</td>
</tr>
<tr>
<td>FPS</td>
<td>页面帧率</td>
</tr>
<tr>
<td>卡顿率</td>
<td>页面是否频繁掉帧</td>
</tr>
<tr>
<td>白屏时间</td>
<td>从打开页面到首屏内容出现的时间</td>
</tr>
</tbody></table>
<p>可以这样讲：</p>
<blockquote>
<p>如果页面资源加载很快，但 JS 执行时间长、主线程被阻塞，用户仍然会感觉页面卡顿，所以还需要监控 Long Task、INP、FPS 等交互体验指标。</p>
</blockquote>
<h3 id="3-接口性能指标"><a href="#3-接口性能指标" class="headerlink" title="3. 接口性能指标"></a>3. 接口性能指标</h3><p>接口性能是前端体验的重要组成部分。</p>
<p>需要监控：</p>
<ol>
<li>接口 URL；</li>
<li>请求方法；</li>
<li>请求耗时；</li>
<li>状态码；</li>
<li>成功 &#x2F; 失败；</li>
<li>错误信息；</li>
<li>慢接口；</li>
<li>超时接口；</li>
<li>重试次数；</li>
<li>业务错误码。</li>
</ol>
<p>实现方式：</p>
<ul>
<li>重写 <code>XMLHttpRequest</code>；</li>
<li>劫持 <code>fetch</code>；</li>
<li>在 Axios 拦截器中统一埋点；</li>
<li>记录请求开始时间、结束时间、状态码和错误信息。</li>
</ul>
<h3 id="4-静态资源性能"><a href="#4-静态资源性能" class="headerlink" title="4. 静态资源性能"></a>4. 静态资源性能</h3><p>需要监控：</p>
<ol>
<li>JS 文件加载耗时；</li>
<li>CSS 文件加载耗时；</li>
<li>图片加载耗时；</li>
<li>字体加载耗时；</li>
<li>资源加载失败；</li>
<li>大资源文件；</li>
<li>CDN 命中情况；</li>
<li>缓存命中情况。</li>
</ol>
<p>资源监控可以通过：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">performance.<span class="title function_">getEntriesByType</span>(<span class="string">&quot;resource&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>获取资源加载信息。</p>
<h3 id="5-前端异常监控"><a href="#5-前端异常监控" class="headerlink" title="5. 前端异常监控"></a>5. 前端异常监控</h3><p>性能监控一般会和异常监控结合起来做。</p>
<p>常见异常包括：</p>
<ol>
<li>JS 运行时错误；</li>
<li>Promise 未捕获异常；</li>
<li>资源加载失败；</li>
<li>接口异常；</li>
<li>框架错误，比如 React Error Boundary；</li>
<li>白屏异常；</li>
<li>页面崩溃；</li>
<li>内存泄漏。</li>
</ol>
<p>常见采集方式：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">window</span>.<span class="property">onerror</span> = <span class="keyword">function</span> (<span class="params">message, source, lineno, colno, error</span>) &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="variable language_">window</span>.<span class="title function_">addEventListener</span>(<span class="string">&quot;unhandledrejection&quot;</span>, <span class="keyword">function</span> (<span class="params">event</span>) &#123;&#125;);</span><br><span class="line"></span><br><span class="line"><span class="variable language_">window</span>.<span class="title function_">addEventListener</span>(<span class="string">&quot;error&quot;</span>, <span class="keyword">function</span> (<span class="params">event</span>) &#123;&#125;, <span class="literal">true</span>);</span><br></pre></td></tr></table></figure>

<h2 id="三、性能数据采集方式"><a href="#三、性能数据采集方式" class="headerlink" title="三、性能数据采集方式"></a>三、性能数据采集方式</h2><h3 id="1-使用-Performance-API"><a href="#1-使用-Performance-API" class="headerlink" title="1. 使用 Performance API"></a>1. 使用 Performance API</h3><p>浏览器提供了原生的性能 API。</p>
<p>常用 API：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">performance.<span class="property">timing</span>;</span><br><span class="line">performance.<span class="title function_">getEntriesByType</span>(<span class="string">&quot;navigation&quot;</span>);</span><br><span class="line">performance.<span class="title function_">getEntriesByType</span>(<span class="string">&quot;resource&quot;</span>);</span><br><span class="line">performance.<span class="title function_">getEntriesByType</span>(<span class="string">&quot;paint&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>现代项目中更推荐：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">performance.<span class="title function_">getEntriesByType</span>(<span class="string">&quot;navigation&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>而不是老的 <code>performance.timing</code>。</p>
<h3 id="2-使用-PerformanceObserver"><a href="#2-使用-PerformanceObserver" class="headerlink" title="2. 使用 PerformanceObserver"></a>2. 使用 PerformanceObserver</h3><p>用于监听性能指标，比如 FCP、LCP、Long Task、CLS 等。</p>
<p>示例：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> observer = <span class="keyword">new</span> <span class="title class_">PerformanceObserver</span>(<span class="function">(<span class="params">list</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">const</span> entry <span class="keyword">of</span> list.<span class="title function_">getEntries</span>()) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(entry);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">observer.<span class="title function_">observe</span>(&#123;</span><br><span class="line">  <span class="attr">type</span>: <span class="string">&quot;largest-contentful-paint&quot;</span>,</span><br><span class="line">  <span class="attr">buffered</span>: <span class="literal">true</span>,</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>前端页面通过 PerformanceObserver 采集性能指标，然后经过上报模块，使用 sendBeacon 或 fetch 把数据发送到监控服务器。服务器记录性能日志后，进入数据分析与可视化阶段，最后通过报表、告警等方式反馈给前端团队，用来指导性能优化。</p>
<table>
<thead>
<tr>
<th>层级</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>前端采集层</td>
<td>监听 LCP、FID、CLS 等性能指标</td>
</tr>
<tr>
<td>传输层</td>
<td>使用 <code>sendBeacon</code> 或 <code>fetch</code> 上报数据</td>
</tr>
<tr>
<td>存储层</td>
<td>记录前端性能日志</td>
</tr>
<tr>
<td>分析层</td>
<td>生成可视化报表，并配置阈值告警</td>
</tr>
<tr>
<td>反馈层</td>
<td>根据监控结果指导前端性能优化</td>
</tr>
</tbody></table>
<h3 id="3-接口请求劫持"><a href="#3-接口请求劫持" class="headerlink" title="3. 接口请求劫持"></a>3. 接口请求劫持</h3><p>如果项目中用 Axios，可以在拦截器中做统计：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">axios.<span class="property">interceptors</span>.<span class="property">request</span>.<span class="title function_">use</span>(<span class="function">(<span class="params">config</span>) =&gt;</span> &#123;</span><br><span class="line">  config.<span class="property">metadata</span> = &#123;</span><br><span class="line">    <span class="attr">startTime</span>: <span class="title class_">Date</span>.<span class="title function_">now</span>(),</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">return</span> config;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">axios.<span class="property">interceptors</span>.<span class="property">response</span>.<span class="title function_">use</span>(</span><br><span class="line">  <span class="function">(<span class="params">response</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> duration = <span class="title class_">Date</span>.<span class="title function_">now</span>() - response.<span class="property">config</span>.<span class="property">metadata</span>.<span class="property">startTime</span>;</span><br><span class="line"></span><br><span class="line">    <span class="title function_">report</span>(&#123;</span><br><span class="line">      <span class="attr">type</span>: <span class="string">&quot;api&quot;</span>,</span><br><span class="line">      <span class="attr">url</span>: response.<span class="property">config</span>.<span class="property">url</span>,</span><br><span class="line">      <span class="attr">method</span>: response.<span class="property">config</span>.<span class="property">method</span>,</span><br><span class="line">      <span class="attr">status</span>: response.<span class="property">status</span>,</span><br><span class="line">      duration,</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> response;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="function">(<span class="params">error</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> config = error.<span class="property">config</span> || &#123;&#125;;</span><br><span class="line">    <span class="keyword">const</span> duration = <span class="title class_">Date</span>.<span class="title function_">now</span>() - config.<span class="property">metadata</span>?.<span class="property">startTime</span>;</span><br><span class="line"></span><br><span class="line">    <span class="title function_">report</span>(&#123;</span><br><span class="line">      <span class="attr">type</span>: <span class="string">&quot;api_error&quot;</span>,</span><br><span class="line">      <span class="attr">url</span>: config.<span class="property">url</span>,</span><br><span class="line">      <span class="attr">method</span>: config.<span class="property">method</span>,</span><br><span class="line">      <span class="attr">status</span>: error.<span class="property">response</span>?.<span class="property">status</span>,</span><br><span class="line">      duration,</span><br><span class="line">      <span class="attr">message</span>: error.<span class="property">message</span>,</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="title class_">Promise</span>.<span class="title function_">reject</span>(error);</span><br><span class="line">  &#125;,</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<h3 id="4-白屏检测"><a href="#4-白屏检测" class="headerlink" title="4. 白屏检测"></a>4. 白屏检测</h3><p>白屏监控一般可以通过关键点采样实现。</p>
<p>思路：</p>
<ol>
<li>页面加载一段时间后；</li>
<li>在屏幕中心区域取多个点；</li>
<li>使用 <code>document.elementFromPoint</code> 判断这些点是否都是空节点；</li>
<li>如果大部分点都是 <code>html</code>、<code>body</code>、空容器，则认为可能白屏；</li>
<li>同时结合 JS 错误、资源加载失败、接口失败一起判断。</li>
</ol>
<p>示例：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">checkWhiteScreen</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> points = [</span><br><span class="line">    [<span class="variable language_">window</span>.<span class="property">innerWidth</span> / <span class="number">2</span>, <span class="variable language_">window</span>.<span class="property">innerHeight</span> / <span class="number">2</span>],</span><br><span class="line">    [<span class="variable language_">window</span>.<span class="property">innerWidth</span> / <span class="number">2</span>, <span class="variable language_">window</span>.<span class="property">innerHeight</span> / <span class="number">4</span>],</span><br><span class="line">    [<span class="variable language_">window</span>.<span class="property">innerWidth</span> / <span class="number">2</span>, (<span class="variable language_">window</span>.<span class="property">innerHeight</span> * <span class="number">3</span>) / <span class="number">4</span>],</span><br><span class="line">    [<span class="variable language_">window</span>.<span class="property">innerWidth</span> / <span class="number">4</span>, <span class="variable language_">window</span>.<span class="property">innerHeight</span> / <span class="number">2</span>],</span><br><span class="line">    [(<span class="variable language_">window</span>.<span class="property">innerWidth</span> * <span class="number">3</span>) / <span class="number">4</span>, <span class="variable language_">window</span>.<span class="property">innerHeight</span> / <span class="number">2</span>],</span><br><span class="line">  ];</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> emptyElements = [<span class="string">&quot;html&quot;</span>, <span class="string">&quot;body&quot;</span>, <span class="string">&quot;#app&quot;</span>, <span class="string">&quot;#root&quot;</span>];</span><br><span class="line"></span><br><span class="line">  <span class="keyword">let</span> emptyCount = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">  points.<span class="title function_">forEach</span>(<span class="function">(<span class="params">[x, y]</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> element = <span class="variable language_">document</span>.<span class="title function_">elementFromPoint</span>(x, y);</span><br><span class="line">    <span class="keyword">if</span> (element &amp;&amp; emptyElements.<span class="title function_">includes</span>(element.<span class="property">tagName</span>.<span class="title function_">toLowerCase</span>())) &#123;</span><br><span class="line">      emptyCount++;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (emptyCount &gt;= <span class="number">4</span>) &#123;</span><br><span class="line">    <span class="title function_">report</span>(&#123;</span><br><span class="line">      <span class="attr">type</span>: <span class="string">&quot;white_screen&quot;</span>,</span><br><span class="line">      <span class="attr">url</span>: location.<span class="property">href</span>,</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">setTimeout</span>(checkWhiteScreen, <span class="number">3000</span>);</span><br></pre></td></tr></table></figure>

<h2 id="四、数据上报方案"><a href="#四、数据上报方案" class="headerlink" title="四、数据上报方案"></a>四、数据上报方案</h2><h3 id="1-上报时机"><a href="#1-上报时机" class="headerlink" title="1. 上报时机"></a>1. 上报时机</h3><p>常见上报时机：</p>
<ol>
<li>页面加载完成后；</li>
<li>页面隐藏时；</li>
<li>页面卸载前；</li>
<li>接口请求结束后；</li>
<li>发生错误时；</li>
<li>达到一定缓存数量后批量上报。</li>
</ol>
<p>推荐：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">navigator.<span class="title function_">sendBeacon</span>();</span><br></pre></td></tr></table></figure>

<p>适合页面关闭、跳转时上报，不容易丢数据。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">navigator.<span class="title function_">sendBeacon</span>(<span class="string">&quot;/monitor/report&quot;</span>, <span class="title class_">JSON</span>.<span class="title function_">stringify</span>(data));</span><br></pre></td></tr></table></figure>

<p>也可以使用图片打点：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> <span class="title class_">Image</span>().<span class="property">src</span> = <span class="string">`/monitor/report?data=<span class="subst">$&#123;<span class="built_in">encodeURIComponent</span>(<span class="built_in">JSON</span>.stringify(data))&#125;</span>`</span>;</span><br></pre></td></tr></table></figure>

<p>或者使用普通 <code>fetch</code> 上报。</p>
<h3 id="2-上报策略"><a href="#2-上报策略" class="headerlink" title="2. 上报策略"></a>2. 上报策略</h3><p>为了避免影响业务性能，需要注意：</p>
<ol>
<li>批量上报，不要每条数据都立即上报；</li>
<li>设置采样率，例如 10% 用户上报；</li>
<li>区分普通用户、灰度用户、内部用户；</li>
<li>错误数据优先上报；</li>
<li>页面卸载时使用 <code>sendBeacon</code>；</li>
<li>上报接口不能阻塞主业务；</li>
<li>上报失败可以做简单重试，但不能无限重试。</li>
</ol>
<h3 id="3-上报数据结构"><a href="#3-上报数据结构" class="headerlink" title="3. 上报数据结构"></a>3. 上报数据结构</h3><p>可以设计成这样：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">type</span>: <span class="string">&#x27;performance&#x27;</span>,</span><br><span class="line">  <span class="attr">appId</span>: <span class="string">&#x27;admin-system&#x27;</span>,</span><br><span class="line">  <span class="attr">userId</span>: <span class="string">&#x27;xxx&#x27;</span>,</span><br><span class="line">  <span class="attr">sessionId</span>: <span class="string">&#x27;xxx&#x27;</span>,</span><br><span class="line">  <span class="attr">pageUrl</span>: location.<span class="property">href</span>,</span><br><span class="line">  <span class="attr">route</span>: <span class="string">&#x27;/dashboard&#x27;</span>,</span><br><span class="line">  <span class="attr">timestamp</span>: <span class="title class_">Date</span>.<span class="title function_">now</span>(),</span><br><span class="line">  <span class="attr">device</span>: &#123;</span><br><span class="line">    <span class="attr">userAgent</span>: navigator.<span class="property">userAgent</span>,</span><br><span class="line">    <span class="attr">screen</span>: <span class="string">`<span class="subst">$&#123;screen.width&#125;</span>x<span class="subst">$&#123;screen.height&#125;</span>`</span>,</span><br><span class="line">    <span class="attr">network</span>: navigator.<span class="property">connection</span>?.<span class="property">effectiveType</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">metrics</span>: &#123;</span><br><span class="line">    <span class="attr">fcp</span>: <span class="number">1200</span>,</span><br><span class="line">    <span class="attr">lcp</span>: <span class="number">2500</span>,</span><br><span class="line">    <span class="attr">ttfb</span>: <span class="number">300</span>,</span><br><span class="line">    <span class="attr">domReady</span>: <span class="number">1800</span>,</span><br><span class="line">    <span class="attr">load</span>: <span class="number">3500</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="五、后端与平台能力"><a href="#五、后端与平台能力" class="headerlink" title="五、后端与平台能力"></a>五、后端与平台能力</h2><p>前端只负责采集和上报，完整方案还需要后端和监控平台支持。</p>
<h3 id="1-后端接收数据"><a href="#1-后端接收数据" class="headerlink" title="1. 后端接收数据"></a>1. 后端接收数据</h3><p>后端需要提供上报接口：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">POST /monitor/report</span><br></pre></td></tr></table></figure>

<p>主要负责：</p>
<ol>
<li>接收前端上报数据；</li>
<li>数据清洗；</li>
<li>数据入库；</li>
<li>数据聚合；</li>
<li>异常告警；</li>
<li>提供查询接口。</li>
</ol>
<h3 id="2-数据存储"><a href="#2-数据存储" class="headerlink" title="2. 数据存储"></a>2. 数据存储</h3><p>可以根据数据量选择：</p>
<table>
<thead>
<tr>
<th>类型</th>
<th>方案</th>
</tr>
</thead>
<tbody><tr>
<td>小规模</td>
<td>MySQL &#x2F; PostgreSQL</td>
</tr>
<tr>
<td>日志型数据</td>
<td>Elasticsearch</td>
</tr>
<tr>
<td>时序数据</td>
<td>Prometheus &#x2F; InfluxDB</td>
</tr>
<tr>
<td>大数据分析</td>
<td>ClickHouse</td>
</tr>
<tr>
<td>日志采集</td>
<td>Kafka + Flink</td>
</tr>
</tbody></table>
<p>前端面试里不用讲太深，可以说：</p>
<blockquote>
<p>数据量小可以先落库，数据量大时可以走日志系统，比如 Kafka + ClickHouse &#x2F; Elasticsearch 做查询分析。</p>
</blockquote>
<h3 id="3-可视化看板"><a href="#3-可视化看板" class="headerlink" title="3. 可视化看板"></a>3. 可视化看板</h3><p>监控平台需要展示：</p>
<ol>
<li>页面平均加载时间；</li>
<li>FCP &#x2F; LCP &#x2F; TTFB 趋势；</li>
<li>慢页面排行；</li>
<li>慢接口排行；</li>
<li>资源加载失败排行；</li>
<li>不同浏览器性能对比；</li>
<li>不同地区性能对比；</li>
<li>不同版本性能对比；</li>
<li>错误率趋势；</li>
<li>白屏率趋势。</li>
</ol>
<h3 id="4-告警机制"><a href="#4-告警机制" class="headerlink" title="4. 告警机制"></a>4. 告警机制</h3><p>可以设置阈值：</p>
<ol>
<li>LCP 超过 4s；</li>
<li>页面白屏率超过 1%；</li>
<li>JS 错误率突然升高；</li>
<li>接口错误率超过 5%；</li>
<li>某接口平均耗时超过 2s；</li>
<li>某版本性能明显劣化。</li>
</ol>
<p>告警渠道：</p>
<ol>
<li>企业微信；</li>
<li>钉钉；</li>
<li>飞书；</li>
<li>邮件；</li>
<li>电话告警。</li>
</ol>
<h2 id="六、项目中可以怎么落地"><a href="#六、项目中可以怎么落地" class="headerlink" title="六、项目中可以怎么落地"></a>六、项目中可以怎么落地</h2><p>实际项目中可以分三层做：</p>
<h3 id="第一层：基础-SDK"><a href="#第一层：基础-SDK" class="headerlink" title="第一层：基础 SDK"></a>第一层：基础 SDK</h3><p>封装一个前端监控 SDK。</p>
<p>负责：</p>
<ol>
<li>初始化配置；</li>
<li>自动采集性能指标；</li>
<li>自动采集错误；</li>
<li>自动采集接口耗时；</li>
<li>自动采集资源加载失败；</li>
<li>数据缓存；</li>
<li>数据上报；</li>
<li>采样控制。</li>
</ol>
<p>示例：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">monitor.<span class="title function_">init</span>(&#123;</span><br><span class="line">  <span class="attr">appId</span>: <span class="string">&quot;digital-human-admin&quot;</span>,</span><br><span class="line">  <span class="attr">env</span>: <span class="string">&quot;production&quot;</span>,</span><br><span class="line">  <span class="attr">userId</span>: <span class="title function_">getUserId</span>(),</span><br><span class="line">  <span class="attr">reportUrl</span>: <span class="string">&quot;/api/monitor/report&quot;</span>,</span><br><span class="line">  <span class="attr">sampleRate</span>: <span class="number">0.1</span>,</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="第二层：业务埋点"><a href="#第二层：业务埋点" class="headerlink" title="第二层：业务埋点"></a>第二层：业务埋点</h3><p>对于关键业务链路，可以手动埋点。</p>
<p>比如：</p>
<ol>
<li>AI 试衣任务创建耗时；</li>
<li>任务轮询成功耗时；</li>
<li>图片生成结果加载耗时；</li>
<li>数字人播报加载耗时；</li>
<li>后台列表查询耗时；</li>
<li>表单提交耗时。</li>
</ol>
<p>示例：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">monitor.<span class="title function_">track</span>(<span class="string">&quot;tryon_task_created&quot;</span>, &#123;</span><br><span class="line">  taskId,</span><br><span class="line">  duration,</span><br><span class="line">  status,</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="第三层：分析治理"><a href="#第三层：分析治理" class="headerlink" title="第三层：分析治理"></a>第三层：分析治理</h3><p>监控不是为了“有数据”，而是为了持续优化。</p>
<p>常见治理方式：</p>
<ol>
<li>慢页面专项优化；</li>
<li>慢接口推动后端优化；</li>
<li>大资源拆分和压缩；</li>
<li>首屏资源按需加载；</li>
<li>路由懒加载；</li>
<li>图片懒加载；</li>
<li>CDN 和缓存优化；</li>
<li>长任务拆分；</li>
<li>降低重复渲染；</li>
<li>定期性能巡检。</li>
</ol>
]]></content>
      <categories>
        <category>编程</category>
      </categories>
      <tags>
        <tag>前端性能</tag>
      </tags>
  </entry>
  <entry>
    <title>记录一些零散的知识点</title>
    <url>/2018/08/06/notes/</url>
    <content><![CDATA[<h3 id="1、JS的for-in循环可以用来遍历对象，用在取用对象中不确定的属性名。"><a href="#1、JS的for-in循环可以用来遍历对象，用在取用对象中不确定的属性名。" class="headerlink" title="1、JS的for in循环可以用来遍历对象，用在取用对象中不确定的属性名。"></a>1、JS的for in循环可以用来遍历对象，用在取用对象中不确定的属性名。</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;<span class="attr">fname</span>:<span class="string">&quot;John&quot;</span>, <span class="attr">lname</span>:<span class="string">&quot;Doe&quot;</span>, <span class="attr">age</span>:<span class="number">25</span>&#125;;</span><br><span class="line"><span class="keyword">var</span> text = <span class="string">&quot;&quot;</span>;</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> key <span class="keyword">in</span> person) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(text += person[key] + <span class="string">&quot; &quot;</span>);<span class="comment">//John Doe 25</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<span id="more"></span>
<h3 id="2、JS中substring与substr的区别"><a href="#2、JS中substring与substr的区别" class="headerlink" title="2、JS中substring与substr的区别"></a>2、JS中substring与substr的区别</h3><h4 id="共同点是都有两个参数，第一个参数的含义都一样，区别是第二个参数：substring为截取到第几位，substr是截取几位，如下："><a href="#共同点是都有两个参数，第一个参数的含义都一样，区别是第二个参数：substring为截取到第几位，substr是截取几位，如下：" class="headerlink" title="共同点是都有两个参数，第一个参数的含义都一样，区别是第二个参数：substring为截取到第几位，substr是截取几位，如下："></a>共同点是都有两个参数，第一个参数的含义都一样，区别是第二个参数：substring为截取到第几位，substr是截取几位，如下：</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> text = <span class="string">&quot;hello world&quot;</span>;</span><br><span class="line">text.<span class="title function_">substr</span>(<span class="number">1</span>,<span class="number">2</span>)</span><br><span class="line"><span class="comment">//el</span></span><br><span class="line">text.<span class="title function_">substring</span>(<span class="number">1</span>,<span class="number">2</span>)</span><br><span class="line"><span class="comment">//e</span></span><br></pre></td></tr></table></figure>
<h3 id="3、Navigator对象的platform和userAgent区别"><a href="#3、Navigator对象的platform和userAgent区别" class="headerlink" title="3、Navigator对象的platform和userAgent区别"></a>3、Navigator对象的platform和userAgent区别</h3><h4 id="platform用于判断操作系统平台，例如："><a href="#platform用于判断操作系统平台，例如：" class="headerlink" title="platform用于判断操作系统平台，例如："></a>platform用于判断操作系统平台，例如：</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> platform = navigator.<span class="property">platform</span>.<span class="title function_">substr</span>(<span class="number">0</span>, <span class="number">3</span>);</span><br><span class="line"><span class="keyword">if</span> (platform === <span class="string">&quot;Win&quot;</span>) &#123;</span><br><span class="line">    <span class="comment">//windows</span></span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (platform === <span class="string">&quot;Mac&quot;</span>) &#123;</span><br><span class="line">    <span class="comment">//macOS</span></span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">    ... ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="UserAgent中文名为用户代理，是Http协议中的一部分，属于头域的组成部分，UserAgent也简称UA。它是一个特殊字符串头，是一种向访问网站提供你所使用的浏览器类型及版本、操作系统及版本、浏览器内核、等信息的标识，一般用于区分用户浏览器的类型及版本，例如："><a href="#UserAgent中文名为用户代理，是Http协议中的一部分，属于头域的组成部分，UserAgent也简称UA。它是一个特殊字符串头，是一种向访问网站提供你所使用的浏览器类型及版本、操作系统及版本、浏览器内核、等信息的标识，一般用于区分用户浏览器的类型及版本，例如：" class="headerlink" title="UserAgent中文名为用户代理，是Http协议中的一部分，属于头域的组成部分，UserAgent也简称UA。它是一个特殊字符串头，是一种向访问网站提供你所使用的浏览器类型及版本、操作系统及版本、浏览器内核、等信息的标识，一般用于区分用户浏览器的类型及版本，例如："></a>UserAgent中文名为用户代理，是Http协议中的一部分，属于头域的组成部分，UserAgent也简称UA。它是一个特殊字符串头，是一种向访问网站提供你所使用的浏览器类型及版本、操作系统及版本、浏览器内核、等信息的标识，一般用于区分用户浏览器的类型及版本，例如：</h4><p>&#x2F;&#x2F;判断IE浏览器版本</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">IEVersion</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">var</span> userAgent = navigator.<span class="property">userAgent</span>; <span class="comment">//取得浏览器的userAgent字符串  </span></span><br><span class="line">    <span class="keyword">var</span> isIE = userAgent.<span class="title function_">indexOf</span>(<span class="string">&quot;compatible&quot;</span>) &gt; -<span class="number">1</span> &amp;&amp; userAgent.<span class="title function_">indexOf</span>(<span class="string">&quot;MSIE&quot;</span>) &gt; -<span class="number">1</span>; <span class="comment">//判断是否IE&lt;11浏览器  </span></span><br><span class="line">    <span class="keyword">var</span> isEdge = userAgent.<span class="title function_">indexOf</span>(<span class="string">&quot;Edge&quot;</span>) &gt; -<span class="number">1</span> &amp;&amp; !isIE; <span class="comment">//判断是否IE的Edge浏览器  </span></span><br><span class="line">    <span class="keyword">var</span> isIE11 = userAgent.<span class="title function_">indexOf</span>(<span class="string">&#x27;Trident&#x27;</span>) &gt; -<span class="number">1</span> &amp;&amp; userAgent.<span class="title function_">indexOf</span>(<span class="string">&quot;rv:11.0&quot;</span>) &gt; -<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">if</span> (isIE) &#123;</span><br><span class="line">        <span class="keyword">var</span> reIE = <span class="keyword">new</span> <span class="title class_">RegExp</span>(<span class="string">&quot;MSIE (\\d+\\.\\d+);&quot;</span>);</span><br><span class="line">        reIE.<span class="title function_">test</span>(userAgent);</span><br><span class="line">        <span class="keyword">var</span> fIEVersion = <span class="built_in">parseFloat</span>(<span class="title class_">RegExp</span>[<span class="string">&quot;$1&quot;</span>]);</span><br><span class="line">        <span class="keyword">if</span> (fIEVersion == <span class="number">7</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">7</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (fIEVersion == <span class="number">8</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">8</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (fIEVersion == <span class="number">9</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">9</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (fIEVersion == <span class="number">10</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">10</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">6</span>;<span class="comment">//IE版本&lt;=7</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (isEdge) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&#x27;edge&#x27;</span>;<span class="comment">//edge</span></span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (isIE11) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">11</span>; <span class="comment">//IE11  </span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> -<span class="number">1</span>;<span class="comment">//不是ie浏览器</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>&#x2F;&#x2F;区分微信和qq</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> ua = navigator.<span class="property">userAgent</span>.<span class="title function_">toLowerCase</span>();</span><br><span class="line"><span class="keyword">if</span> (ua.<span class="title function_">indexOf</span>(<span class="string">&quot;micromessenger&quot;</span>) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">//微信</span></span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (ua.<span class="title function_">indexOf</span>(<span class="string">&quot;qq&quot;</span>) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">//QQ</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="4、number-toLocaleString"><a href="#4、number-toLocaleString" class="headerlink" title="4、number.toLocaleString()"></a>4、number.toLocaleString()</h3><h4 id="toLocaleString-方法可把一个-Number-对象转换为本地格式的字符串，常用于数值转化成千分位，如下："><a href="#toLocaleString-方法可把一个-Number-对象转换为本地格式的字符串，常用于数值转化成千分位，如下：" class="headerlink" title="toLocaleString() 方法可把一个 Number 对象转换为本地格式的字符串，常用于数值转化成千分位，如下："></a>toLocaleString() 方法可把一个 Number 对象转换为本地格式的字符串，常用于数值转化成千分位，如下：</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> num = <span class="number">1231231231</span>;</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(num.<span class="title function_">toLocaleString</span>());<span class="comment">//1,231,231,231</span></span><br></pre></td></tr></table></figure>
<h3 id="5、P标签中不能嵌套块级标签"><a href="#5、P标签中不能嵌套块级标签" class="headerlink" title="5、P标签中不能嵌套块级标签"></a>5、P标签中不能嵌套块级标签</h3><h3 id="6、http请求url传参时的中文编码问题"><a href="#6、http请求url传参时的中文编码问题" class="headerlink" title="6、http请求url传参时的中文编码问题"></a>6、http请求url传参时的中文编码问题</h3><h4 id="项目中遇到在一个url传参的get请求里，安卓机上中文参数会乱码，ios是好的，使用encodeURI-解决了问题，encodeURI-函数可把字符串作为-URI-进行编码，用法如下："><a href="#项目中遇到在一个url传参的get请求里，安卓机上中文参数会乱码，ios是好的，使用encodeURI-解决了问题，encodeURI-函数可把字符串作为-URI-进行编码，用法如下：" class="headerlink" title="项目中遇到在一个url传参的get请求里，安卓机上中文参数会乱码，ios是好的，使用encodeURI()解决了问题，encodeURI() 函数可把字符串作为 URI 进行编码，用法如下："></a>项目中遇到在一个url传参的get请求里，安卓机上中文参数会乱码，ios是好的，使用<code>encodeURI()</code>解决了问题，encodeURI() 函数可把字符串作为 URI 进行编码，用法如下：</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">encodeURI(getApp().globalData.hempConfig.agentBasePath+&#x27;/sec/school/getAgentSchoolInfo&#x27;+strParam)</span><br></pre></td></tr></table></figure>
<h4 id="这样就可以保证中文参数的准确性"><a href="#这样就可以保证中文参数的准确性" class="headerlink" title="这样就可以保证中文参数的准确性"></a>这样就可以保证中文参数的准确性</h4>]]></content>
      <categories>
        <category>编程</category>
      </categories>
      <tags>
        <tag>js html css</tag>
      </tags>
  </entry>
  <entry>
    <title>React的Ref属性在TypeScript中的写法</title>
    <url>/2018/05/04/reactRefs/</url>
    <content><![CDATA[<h4 id="在TypeScript中使用ref属性的时候，如果不定义它的类型直接去使用this-refs-XXX，就会在编译的时候报错。参考如下代码："><a href="#在TypeScript中使用ref属性的时候，如果不定义它的类型直接去使用this-refs-XXX，就会在编译的时候报错。参考如下代码：" class="headerlink" title="在TypeScript中使用ref属性的时候，如果不定义它的类型直接去使用this.refs.XXX，就会在编译的时候报错。参考如下代码："></a>在TypeScript中使用ref属性的时候，如果不定义它的类型直接去使用this.refs.XXX，就会在编译的时候报错。参考如下代码：</h4><span id="more"></span>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> <span class="title class_">React</span> <span class="keyword">from</span> <span class="string">&quot;react&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">CustomTextInput</span> <span class="keyword">extends</span> <span class="title class_ inherited__">React.Component</span>&lt;&#123;&#125;, &#123;&#125;&gt; &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//在此处统一定义refs</span></span><br><span class="line">    <span class="keyword">public</span> <span class="attr">refs</span>: &#123;</span><br><span class="line">        <span class="attr">textInput</span>: <span class="title class_">HTMLInputElement</span>,</span><br><span class="line">        <span class="attr">loadMoreDiv</span>: <span class="title class_">HTMLElement</span></span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="title function_">constructor</span>(<span class="params"></span>) &#123;</span><br><span class="line">        <span class="variable language_">super</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="title function_">componentDidMount</span>(<span class="params"></span>) &#123;</span><br><span class="line">        <span class="keyword">const</span> loadMore = <span class="variable language_">this</span>.<span class="property">refs</span>.<span class="property">loadMoreDiv</span>;</span><br><span class="line">        <span class="keyword">const</span> myInput = <span class="variable language_">this</span>.<span class="property">refs</span>.<span class="property">textInput</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">render</span>(<span class="params"></span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">            <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;text&quot;</span> <span class="attr">ref</span>= <span class="string">&quot;textInput&quot;</span> /&gt;</span></span></span><br><span class="line"><span class="language-xml">            <span class="tag">&lt;<span class="name">div</span> <span class="attr">ref</span>= <span class="string">&quot;loadMoreDiv&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">        <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>转自：<a href="https://goenning.net/2016/11/02/strongly-typed-react-refs-with-typescript/">Strongly Typed React Refs with TypeScript</a></p>
]]></content>
      <categories>
        <category>编程</category>
      </categories>
      <tags>
        <tag>React</tag>
      </tags>
  </entry>
  <entry>
    <title>react如何在组件外部调用组件内的方法</title>
    <url>/2018/07/04/reactTechnology/</url>
    <content><![CDATA[<h4 id="在开发中，难免会遇到这种场景：我们需要在组件外部调用组件内的方法，从而达到在外部操作组件或者获取组件内部数据的目的。"><a href="#在开发中，难免会遇到这种场景：我们需要在组件外部调用组件内的方法，从而达到在外部操作组件或者获取组件内部数据的目的。" class="headerlink" title="在开发中，难免会遇到这种场景：我们需要在组件外部调用组件内的方法，从而达到在外部操作组件或者获取组件内部数据的目的。"></a>在开发中，难免会遇到这种场景：我们需要在组件外部调用组件内的方法，从而达到在外部操作组件或者获取组件内部数据的目的。</h4><h4 id="示例代码如下："><a href="#示例代码如下：" class="headerlink" title="示例代码如下："></a>示例代码如下：</h4><span id="more"></span>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> <span class="title class_">React</span> <span class="keyword">from</span> <span class="string">&quot;react&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//外层组件</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">Parent</span> <span class="keyword">extends</span> <span class="title class_ inherited__">React.Component</span>&lt;&#123;&#125;, &#123;&#125;&gt; &#123;</span><br><span class="line">    <span class="comment">//用一个变量接收子组件的this</span></span><br><span class="line">    <span class="keyword">public</span> <span class="attr">child</span>: <span class="built_in">any</span>;</span><br><span class="line">    <span class="title function_">constructor</span>(<span class="params"></span>) &#123;</span><br><span class="line">        <span class="variable language_">super</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//绑定子组件的this到this.child</span></span><br><span class="line">    bindChild = <span class="function">(<span class="params"><span class="attr">ref</span>:<span class="built_in">any</span></span>) =&gt;</span> &#123;</span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">child</span> = ref;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//用于调用子组件的方法</span></span><br><span class="line">    click = <span class="function">(<span class="params">e</span>) =&gt;</span> &#123;</span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">child</span>.<span class="title function_">myName</span>()</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">render</span>(<span class="params"></span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">            <span class="tag">&lt;<span class="name">Child</span> <span class="attr">bindChild</span>=<span class="string">&#123;this.bindChild&#125;</span> /&gt;</span></span></span><br><span class="line"><span class="language-xml">            <span class="tag">&lt;<span class="name">button</span> <span class="attr">onClick</span>=<span class="string">&#123;this.click&#125;</span> &gt;</span>getName<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">        <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//子组件</span></span><br><span class="line"><span class="keyword">interface</span> <span class="title class_">ChildOwnProps</span> &#123;</span><br><span class="line">    <span class="attr">bindChild</span>: <span class="title class_">Function</span>;<span class="comment">//提供一个属性，用于把this传递给外部组件</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Child</span> <span class="keyword">extends</span> <span class="title class_ inherited__">React.Component</span>&lt;<span class="title class_">ChildOwnProps</span>&gt; &#123;</span><br><span class="line">    <span class="title function_">constructor</span>(<span class="params"><span class="attr">props</span>:<span class="title class_">ChildOwnProps</span></span>) &#123;</span><br><span class="line">        <span class="variable language_">super</span>(props);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="title function_">componentDidMount</span>(<span class="params"></span>)&#123;</span><br><span class="line">        <span class="comment">//传递this</span></span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">props</span>.<span class="title function_">bindChild</span>(<span class="variable language_">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//供外部调用的方法</span></span><br><span class="line">    myName = <span class="function">() =&gt;</span> <span class="title function_">alert</span>(<span class="string">&#x27;xiaohesong&#x27;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="title function_">render</span>(<span class="params"></span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="language-xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span>child<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h4 id="其实原理很简单，即子组件向外提供一个属性，用于把自己的this传递给外部组件，外部组件用一个变量接收子组件传递过来的this之后就可以随意调用子组件内部的方法。"><a href="#其实原理很简单，即子组件向外提供一个属性，用于把自己的this传递给外部组件，外部组件用一个变量接收子组件传递过来的this之后就可以随意调用子组件内部的方法。" class="headerlink" title="其实原理很简单，即子组件向外提供一个属性，用于把自己的this传递给外部组件，外部组件用一个变量接收子组件传递过来的this之后就可以随意调用子组件内部的方法。"></a>其实原理很简单，即子组件向外提供一个属性，用于把自己的this传递给外部组件，外部组件用一个变量接收子组件传递过来的this之后就可以随意调用子组件内部的方法。</h4>]]></content>
      <categories>
        <category>编程</category>
      </categories>
      <tags>
        <tag>React</tag>
      </tags>
  </entry>
  <entry>
    <title>一切都会变得更美好</title>
    <url>/2024/07/10/someTalkingShit/</url>
    <content><![CDATA[<h3 id="假如生活欺骗了你，"><a href="#假如生活欺骗了你，" class="headerlink" title="假如生活欺骗了你，"></a><center>假如生活欺骗了你，</center></h3><span id="more"></span>
<h3 id="不要悲伤，不要心急！"><a href="#不要悲伤，不要心急！" class="headerlink" title="不要悲伤，不要心急！"></a><center>不要悲伤，不要心急！</center></h3><h3 id="忧郁的日子里须要镇静："><a href="#忧郁的日子里须要镇静：" class="headerlink" title="忧郁的日子里须要镇静："></a><center>忧郁的日子里须要镇静：</center></h3><h3 id="相信吧，快乐的日子将会来临！"><a href="#相信吧，快乐的日子将会来临！" class="headerlink" title="相信吧，快乐的日子将会来临！"></a><center>相信吧，快乐的日子将会来临！</center></h3><h3 id="心儿永远向往着未来；"><a href="#心儿永远向往着未来；" class="headerlink" title="心儿永远向往着未来；"></a><center>心儿永远向往着未来；</center></h3><h3 id="现在却常是忧郁。"><a href="#现在却常是忧郁。" class="headerlink" title="现在却常是忧郁。"></a><center>现在却常是忧郁。</center></h3><h3 id="一切都是瞬息，一切都将会过去；"><a href="#一切都是瞬息，一切都将会过去；" class="headerlink" title="一切都是瞬息，一切都将会过去；"></a><center>一切都是瞬息，一切都将会过去；</center></h3><h3 id="而那过去了的，就会成为亲切的怀恋。"><a href="#而那过去了的，就会成为亲切的怀恋。" class="headerlink" title="而那过去了的，就会成为亲切的怀恋。"></a><center>而那过去了的，就会成为亲切的怀恋。</center></h3><h3 id=""><a href="#" class="headerlink" title=""></a><br/></h3><h3 id="总会好起来的，不是吗？"><a href="#总会好起来的，不是吗？" class="headerlink" title="总会好起来的，不是吗？"></a><center>总会好起来的，不是吗？</center></h3><img src="/2024/07/10/someTalkingShit/image.png" class="" title="Everything will be all right.">]]></content>
      <categories>
        <category>生活</category>
      </categories>
      <tags>
        <tag>Life</tag>
      </tags>
  </entry>
  <entry>
    <title>SSE流式对话</title>
    <url>/2026/07/16/sse-streaming-chat/</url>
    <content><![CDATA[<h1 id="SSE（Server-Sent-Events）流式对话"><a href="#SSE（Server-Sent-Events）流式对话" class="headerlink" title="SSE（Server-Sent Events）流式对话"></a>SSE（Server-Sent Events）流式对话</h1><h2 id="什么是SSE"><a href="#什么是SSE" class="headerlink" title="什么是SSE"></a>什么是SSE</h2><p>SSE（Server-Sent Events）是一种基于HTTP的<strong>服务器推送技术</strong>，客户端发起一次请求后，服务器保持连接并持续向客户端发送数据，无需客户端不断轮询。</p>
<p><strong>一句话：</strong></p>
<blockquote>
<p><strong>客户端请求一次，服务器持续推送数据，直到响应结束。</strong></p>
</blockquote>
<span id="more"></span>

<hr>
<h1 id="为什么AI对话使用SSE"><a href="#为什么AI对话使用SSE" class="headerlink" title="为什么AI对话使用SSE"></a>为什么AI对话使用SSE</h1><p>普通HTTP请求：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">用户发送消息</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">等待模型全部生成完成</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">一次返回完整内容</span><br></pre></td></tr></table></figure>

<p>SSE流式返回：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">用户发送消息</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">服务器开始生成</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">你</span><br><span class="line">你好</span><br><span class="line">你好！</span><br><span class="line">你好！很高兴……</span><br></pre></td></tr></table></figure>

<p>前端边接收边渲染，实现类似”打字机”效果，用户体验更好。</p>
<hr>
<h1 id="SSE通信流程"><a href="#SSE通信流程" class="headerlink" title="SSE通信流程"></a>SSE通信流程</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Browser</span><br><span class="line">    │</span><br><span class="line">    │ POST /chat</span><br><span class="line">    ▼</span><br><span class="line">Server</span><br><span class="line">────────────────────────────────────</span><br><span class="line">保持HTTP连接</span><br><span class="line"></span><br><span class="line">data: &#123;&quot;content&quot;:&quot;你&quot;&#125;</span><br><span class="line"></span><br><span class="line">data: &#123;&quot;content&quot;:&quot;好&quot;&#125;</span><br><span class="line"></span><br><span class="line">data: &#123;&quot;content&quot;:&quot;！&quot;&#125;</span><br><span class="line"></span><br><span class="line">data: [DONE]</span><br><span class="line"></span><br><span class="line">────────────────────────────────────</span><br><span class="line">连接关闭</span><br></pre></td></tr></table></figure>

<p>整个过程中：</p>
<ul>
<li>客户端发送一次请求</li>
<li>服务器保持连接持续推送数据</li>
<li>前端实时接收并渲染</li>
<li>收到<code>[DONE]</code>（或服务端约定的结束标识）后结束响应</li>
</ul>
<hr>
<h1 id="服务端实现（关键响应头）"><a href="#服务端实现（关键响应头）" class="headerlink" title="服务端实现（关键响应头）"></a>服务端实现（关键响应头）</h1><p>服务端需要设置SSE相关响应头：</p>
<figure class="highlight http"><table><tr><td class="code"><pre><span class="line"><span class="attribute">Content-Type</span><span class="punctuation">: </span>text/event-stream</span><br><span class="line"><span class="attribute">Cache-Control</span><span class="punctuation">: </span>no-cache</span><br><span class="line"><span class="attribute">Connection</span><span class="punctuation">: </span>keep-alive</span><br></pre></td></tr></table></figure>

<p>作用：</p>
<ul>
<li><strong>Content-Type: text&#x2F;event-stream</strong>：声明响应为SSE流。</li>
<li><strong>Cache-Control: no-cache</strong>：防止浏览器缓存。</li>
<li><strong>Connection: keep-alive</strong>：保持HTTP长连接（HTTP&#x2F;1.1下常见，HTTP&#x2F;2下意义较小）。</li>
</ul>
<p>服务端持续发送数据：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">res.<span class="title function_">write</span>(<span class="string">`data: <span class="subst">$&#123;<span class="built_in">JSON</span>.stringify(data)&#125;</span>\n\n`</span>);</span><br></pre></td></tr></table></figure>

<p>最后结束：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">res.<span class="title function_">write</span>(<span class="string">&quot;data: [DONE]\n\n&quot;</span>);</span><br><span class="line">res.<span class="title function_">end</span>();</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="SSE数据格式"><a href="#SSE数据格式" class="headerlink" title="SSE数据格式"></a>SSE数据格式</h1><p>服务器返回的数据必须遵循SSE协议。</p>
<p>例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">data: hello</span><br><span class="line"></span><br><span class="line">data: world</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>AI接口通常返回：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">data: &#123;&quot;content&quot;:&quot;你&quot;&#125;</span><br><span class="line"></span><br><span class="line">data: &#123;&quot;content&quot;:&quot;好&quot;&#125;</span><br><span class="line"></span><br><span class="line">data: &#123;&quot;content&quot;:&quot;！&quot;&#125;</span><br><span class="line"></span><br><span class="line">data: [DONE]</span><br></pre></td></tr></table></figure>

<p>其中：</p>
<ul>
<li><code>data:</code>：消息内容</li>
<li>空行：表示一条消息结束</li>
<li><code>[DONE]</code>：流结束标识（由服务端定义，并非SSE协议强制要求）</li>
</ul>
<hr>
<h1 id="前端如何接收SSE"><a href="#前端如何接收SSE" class="headerlink" title="前端如何接收SSE"></a>前端如何接收SSE</h1><p>目前主要有两种方式。</p>
<h2 id="方式一：EventSource（原生SSE）"><a href="#方式一：EventSource（原生SSE）" class="headerlink" title="方式一：EventSource（原生SSE）"></a>方式一：EventSource（原生SSE）</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> es = <span class="keyword">new</span> <span class="title class_">EventSource</span>(<span class="string">&quot;/chat&quot;</span>);</span><br><span class="line"></span><br><span class="line">es.<span class="property">onmessage</span> = <span class="function">(<span class="params">e</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(e.<span class="property">data</span>);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul>
<li>浏览器原生支持</li>
<li>自动重连</li>
<li>使用简单</li>
</ul>
<h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><p>只能支持：</p>
<ul>
<li>GET请求</li>
</ul>
<p>不能支持：</p>
<ul>
<li>POST</li>
<li>Request Body</li>
<li>自定义Authorization Header</li>
</ul>
<p>因此现代AI项目很少直接使用。</p>
<hr>
<h2 id="方式二：fetch-ReadableStream（推荐）"><a href="#方式二：fetch-ReadableStream（推荐）" class="headerlink" title="方式二：fetch + ReadableStream（推荐）"></a>方式二：fetch + ReadableStream（推荐）</h2><p>现代AI（OpenAI、DeepSeek、Claude等）基本都采用这种方式。</p>
<p>发送请求：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> response = <span class="keyword">await</span> <span class="title function_">fetch</span>(<span class="string">&quot;/chat&quot;</span>, &#123;</span><br><span class="line">  <span class="attr">method</span>: <span class="string">&quot;POST&quot;</span>,</span><br><span class="line">  <span class="attr">headers</span>: &#123;</span><br><span class="line">    <span class="title class_">Authorization</span>: <span class="string">&quot;Bearer xxx&quot;</span>,</span><br><span class="line">    <span class="string">&quot;Content-Type&quot;</span>: <span class="string">&quot;application/json&quot;</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">body</span>: <span class="title class_">JSON</span>.<span class="title function_">stringify</span>(&#123;</span><br><span class="line">    <span class="attr">message</span>: <span class="string">&quot;你好&quot;</span>,</span><br><span class="line">  &#125;),</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>读取流：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> reader = response.<span class="property">body</span>.<span class="title function_">getReader</span>();</span><br><span class="line"><span class="keyword">const</span> decoder = <span class="keyword">new</span> <span class="title class_">TextDecoder</span>();</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> &#123; done, value &#125; = <span class="keyword">await</span> reader.<span class="title function_">read</span>();</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (done) <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> text = decoder.<span class="title function_">decode</span>(value, &#123;</span><br><span class="line">    <span class="attr">stream</span>: <span class="literal">true</span>,</span><br><span class="line">  &#125;);</span><br><span class="line"></span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(text);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>优势：</p>
<ul>
<li>支持POST</li>
<li>支持Token认证</li>
<li>支持Request Body</li>
<li>支持聊天历史、模型参数等复杂数据</li>
<li>完全兼容REST API</li>
<li>更符合现代AI接口设计</li>
</ul>
<hr>
<h1 id="为什么AI接口使用POST"><a href="#为什么AI接口使用POST" class="headerlink" title="为什么AI接口使用POST"></a>为什么AI接口使用POST</h1><p>AI接口通常需要提交大量参数，例如：</p>
<ul>
<li>用户消息</li>
<li>聊天历史（History）</li>
<li>System Prompt</li>
<li>Temperature</li>
<li>Top P</li>
<li>最大Token数</li>
<li>Token认证</li>
</ul>
<p>因此通常采用：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">POST</span><br><span class="line">      +</span><br><span class="line">Request Body</span><br><span class="line">      +</span><br><span class="line">fetch</span><br><span class="line">      +</span><br><span class="line">ReadableStream</span><br></pre></td></tr></table></figure>

<p>而不是<code>EventSource</code>的GET请求。</p>
<hr>
<h1 id="ReadableStream"><a href="#ReadableStream" class="headerlink" title="ReadableStream"></a>ReadableStream</h1><p><code>fetch</code>返回的响应体是一个<code>ReadableStream</code>。</p>
<p>通过：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">response.<span class="property">body</span>.<span class="title function_">getReader</span>();</span><br></pre></td></tr></table></figure>

<p>获取Reader。</p>
<p>循环读取：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">reader.<span class="title function_">read</span>();</span><br></pre></td></tr></table></figure>

<p>返回：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">done</span>: <span class="literal">false</span>,</span><br><span class="line">  <span class="attr">value</span>: <span class="title class_">Uint8Array</span>(...)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其中：</p>
<ul>
<li>value：当前数据块（Chunk）</li>
<li>done：是否读取完成</li>
</ul>
<hr>
<h1 id="什么是Chunk"><a href="#什么是Chunk" class="headerlink" title="什么是Chunk"></a>什么是Chunk</h1><p>网络不会一次返回完整数据，而是分成多个<strong>Chunk（数据块）</strong>进行传输。</p>
<p>例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Chunk1</span><br><span class="line">↓</span><br><span class="line"></span><br><span class="line">Chunk2</span><br><span class="line">↓</span><br><span class="line"></span><br><span class="line">Chunk3</span><br><span class="line">↓</span><br><span class="line"></span><br><span class="line">......</span><br></pre></td></tr></table></figure>

<p>浏览器通过：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">reader.<span class="title function_">read</span>();</span><br></pre></td></tr></table></figure>

<p>一次读取一个Chunk，因此页面能够边接收边显示。</p>
<p>需要注意的是：</p>
<blockquote>
<p><strong>Chunk并不等于Token，一个Chunk可能包含一个Token、多个Token，甚至一整段文本，具体由服务端决定。</strong></p>
</blockquote>
<hr>
<h1 id="TextDecoder"><a href="#TextDecoder" class="headerlink" title="TextDecoder"></a>TextDecoder</h1><p>服务器发送的是字节流，而不是字符串。</p>
<p>例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">228 189 160</span><br></pre></td></tr></table></figure>

<p>浏览器读取后得到：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title class_">Uint8Array</span>(...)</span><br></pre></td></tr></table></figure>

<p>需要使用：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> decoder = <span class="keyword">new</span> <span class="title class_">TextDecoder</span>();</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> text = decoder.<span class="title function_">decode</span>(value);</span><br></pre></td></tr></table></figure>

<p>转换成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">你</span><br></pre></td></tr></table></figure>

<p>所以：</p>
<blockquote>
<p><strong>TextDecoder负责将Uint8Array解码成字符串。</strong></p>
</blockquote>
<hr>
<h1 id="为什么使用stream-true（重点）"><a href="#为什么使用stream-true（重点）" class="headerlink" title="为什么使用stream: true（重点）"></a>为什么使用stream: true（重点）</h1><p>推荐：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">decoder.<span class="title function_">decode</span>(value, &#123;</span><br><span class="line">  <span class="attr">stream</span>: <span class="literal">true</span>,</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>原因：</p>
<p>网络数据按Chunk传输，一个中文字符可能被拆成两块。</p>
<p>例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">第一次：</span><br><span class="line">228 189</span><br><span class="line"></span><br><span class="line">第二次：</span><br><span class="line">160</span><br></pre></td></tr></table></figure>

<p>如果直接：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">decoder.<span class="title function_">decode</span>(value);</span><br></pre></td></tr></table></figure>

<p>可能出现乱码：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">�</span><br></pre></td></tr></table></figure>

<p>使用：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">decoder.<span class="title function_">decode</span>(value, &#123;</span><br><span class="line">  <span class="attr">stream</span>: <span class="literal">true</span>,</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>会自动缓存不完整字节，等待下一块数据后一起解码，避免乱码。</p>
<hr>
<h1 id="停止生成（AbortController）"><a href="#停止生成（AbortController）" class="headerlink" title="停止生成（AbortController）"></a>停止生成（AbortController）</h1><p>AI聊天中的”停止生成”通常通过<code>AbortController</code>实现。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> controller = <span class="keyword">new</span> <span class="title class_">AbortController</span>();</span><br><span class="line"></span><br><span class="line"><span class="title function_">fetch</span>(<span class="string">&quot;/chat&quot;</span>, &#123;</span><br><span class="line">  <span class="attr">signal</span>: controller.<span class="property">signal</span>,</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 停止生成</span></span><br><span class="line">controller.<span class="title function_">abort</span>();</span><br></pre></td></tr></table></figure>

<p>作用：</p>
<ul>
<li>中断HTTP请求</li>
<li>关闭SSE连接</li>
<li>停止继续接收模型返回的数据</li>
</ul>
<hr>
<h1 id="心跳（了解）"><a href="#心跳（了解）" class="headerlink" title="心跳（了解）"></a>心跳（了解）</h1><p>为了避免长连接超时，服务端可能会定时发送心跳包，例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">:</span><br></pre></td></tr></table></figure>

<p>或者：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">event: ping</span><br><span class="line">data: ping</span><br></pre></td></tr></table></figure>

<p>作用：</p>
<ul>
<li>保持连接不断开</li>
<li>防止代理服务器、负载均衡等主动关闭空闲连接</li>
</ul>
<p>一般由服务端实现，前端通常无需处理。</p>
<hr>
<h1 id="完整流程"><a href="#完整流程" class="headerlink" title="完整流程"></a>完整流程</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">用户输入问题</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">fetch发送POST请求</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">服务器调用AI模型</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">模型生成内容</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">按多个Chunk持续推送</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">ReadableStream读取Chunk</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">TextDecoder解码</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">解析data:</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">JSON.parse()</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">拼接内容</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">更新页面（流式输出）</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="SSE与WebSocket对比"><a href="#SSE与WebSocket对比" class="headerlink" title="SSE与WebSocket对比"></a>SSE与WebSocket对比</h1><table>
<thead>
<tr>
<th>对比项</th>
<th>SSE</th>
<th>WebSocket</th>
</tr>
</thead>
<tbody><tr>
<td>通信方向</td>
<td>单向（服务端→客户端）</td>
<td>双向通信</td>
</tr>
<tr>
<td>协议</td>
<td>HTTP</td>
<td>WebSocket</td>
</tr>
<tr>
<td>建立连接</td>
<td>简单</td>
<td>需握手</td>
</tr>
<tr>
<td>自动重连</td>
<td>支持（EventSource）</td>
<td>需自行实现</td>
</tr>
<tr>
<td>是否适合流式输出</td>
<td>⭐⭐⭐⭐⭐</td>
<td>⭐⭐⭐</td>
</tr>
<tr>
<td>聊天室、游戏</td>
<td>⭐⭐</td>
<td>⭐⭐⭐⭐⭐</td>
</tr>
</tbody></table>
<h3 id="为什么AI更喜欢SSE？"><a href="#为什么AI更喜欢SSE？" class="headerlink" title="为什么AI更喜欢SSE？"></a>为什么AI更喜欢SSE？</h3><p>AI对话的通信模式基本都是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">客户端发送一次问题</span><br><span class="line">        │</span><br><span class="line">        ▼</span><br><span class="line">服务器持续返回答案</span><br></pre></td></tr></table></figure>

<p>客户端几乎没有持续发送数据的需求，因此SSE更加轻量、简单，也更符合HTTP生态。</p>
<hr>
<h1 id="SSE的局限性"><a href="#SSE的局限性" class="headerlink" title="SSE的局限性"></a>SSE的局限性</h1><ul>
<li>仅支持服务端向客户端单向通信。</li>
<li>浏览器连接数受HTTP协议限制（HTTP&#x2F;2下已明显改善）。</li>
<li>不适合聊天室、在线游戏、协同编辑等双向实时通信场景。</li>
<li>若需要客户端频繁主动推送数据，更适合使用WebSocket。</li>
</ul>
<hr>
<h1 id="面试高频问题"><a href="#面试高频问题" class="headerlink" title="面试高频问题"></a>面试高频问题</h1><h3 id="①-什么是SSE？"><a href="#①-什么是SSE？" class="headerlink" title="① 什么是SSE？"></a>① 什么是SSE？</h3><blockquote>
<p>SSE（Server-Sent Events）是一种基于HTTP的服务器推送技术，客户端发起一次请求后，服务器保持连接并持续向客户端发送数据，非常适合AI聊天、日志推送、任务进度等单向推送场景。</p>
</blockquote>
<hr>
<h3 id="②-为什么AI聊天喜欢使用SSE？"><a href="#②-为什么AI聊天喜欢使用SSE？" class="headerlink" title="② 为什么AI聊天喜欢使用SSE？"></a>② 为什么AI聊天喜欢使用SSE？</h3><ul>
<li>支持边生成边返回，降低等待时间。</li>
<li>实现打字机效果，用户体验更好。</li>
<li>基于HTTP，实现简单，兼容现有鉴权、代理和网关体系。</li>
<li>AI问答主要是单向推送，非常符合SSE使用场景。</li>
</ul>
<hr>
<h3 id="③-为什么不用EventSource？"><a href="#③-为什么不用EventSource？" class="headerlink" title="③ 为什么不用EventSource？"></a>③ 为什么不用EventSource？</h3><p>因为<code>EventSource</code>只能发送GET请求，不支持：</p>
<ul>
<li>POST</li>
<li>Request Body</li>
<li>Authorization Header</li>
</ul>
<p>现代AI接口通常需要POST请求和Token认证，因此普遍采用<strong>fetch + ReadableStream</strong>实现流式响应。</p>
<hr>
<h3 id="④-ReadableStream是什么？"><a href="#④-ReadableStream是什么？" class="headerlink" title="④ ReadableStream是什么？"></a>④ ReadableStream是什么？</h3><blockquote>
<p><code>ReadableStream</code>表示可读数据流，<code>fetch</code>返回的响应体就是一个<code>ReadableStream</code>。通过<code>getReader().read()</code>可以逐块（Chunk）读取服务器返回的数据，实现流式处理。</p>
</blockquote>
<hr>
<h3 id="⑤-TextDecoder有什么作用？"><a href="#⑤-TextDecoder有什么作用？" class="headerlink" title="⑤ TextDecoder有什么作用？"></a>⑤ TextDecoder有什么作用？</h3><blockquote>
<p><code>TextDecoder</code>用于将<code>Uint8Array</code>等二进制数据按照UTF-8解码为字符串。在SSE中，浏览器读取到的是字节流，需要通过<code>TextDecoder.decode()</code>转换为文本，再解析和渲染。</p>
</blockquote>
<hr>
<h3 id="⑥-为什么推荐使用stream-true？"><a href="#⑥-为什么推荐使用stream-true？" class="headerlink" title="⑥ 为什么推荐使用stream: true？"></a>⑥ 为什么推荐使用<code>stream: true</code>？</h3><blockquote>
<p>网络传输按Chunk进行，一个中文字符可能跨多个Chunk。<code>stream: true</code>会缓存不完整字节，等待下一块数据后再解码，避免中文乱码。</p>
</blockquote>
<hr>
<h3 id="⑦-AbortController有什么作用？"><a href="#⑦-AbortController有什么作用？" class="headerlink" title="⑦ AbortController有什么作用？"></a>⑦ AbortController有什么作用？</h3><blockquote>
<p><code>AbortController</code>用于取消<code>fetch</code>请求。在AI聊天中通常用于实现”停止生成”，调用<code>abort()</code>即可中断请求并关闭SSE连接。</p>
</blockquote>
<hr>
<h1 id="一句话总结"><a href="#一句话总结" class="headerlink" title="一句话总结"></a>一句话总结</h1><blockquote>
<p><strong>现代AI流式对话通常采用<code>fetch + ReadableStream + TextDecoder + SSE</code>方案：客户端通过<code>fetch</code>发送POST请求，服务器以SSE格式持续推送数据，前端利用<code>ReadableStream</code>逐块读取Chunk，再通过<code>TextDecoder</code>解码为字符串，解析数据后实时更新页面，实现流式输出的”打字机”效果。</strong></p>
</blockquote>
]]></content>
  </entry>
  <entry>
    <title>小程序开发笔记</title>
    <url>/2018/11/28/weapp/</url>
    <content><![CDATA[<h2 id="在小程序里如何关注公众号（公众号如何关联小程序）"><a href="#在小程序里如何关注公众号（公众号如何关联小程序）" class="headerlink" title="在小程序里如何关注公众号（公众号如何关联小程序）"></a>在小程序里如何关注公众号（公众号如何关联小程序）</h2><h4 id="首先你需要在公众号里给小程序发送一个关联邀请，如下图："><a href="#首先你需要在公众号里给小程序发送一个关联邀请，如下图：" class="headerlink" title="首先你需要在公众号里给小程序发送一个关联邀请，如下图："></a>首先你需要在公众号里给小程序发送一个关联邀请，如下图：</h4><img src="/2018/11/28/weapp/yaoqing.png" class="" title="yaoqing">
<h4 id="发送之后小程序的管理员会在微信端收到一条邀请通知，点开消息之后点击确认就行，如下图："><a href="#发送之后小程序的管理员会在微信端收到一条邀请通知，点开消息之后点击确认就行，如下图：" class="headerlink" title="发送之后小程序的管理员会在微信端收到一条邀请通知，点开消息之后点击确认就行，如下图："></a>发送之后小程序的管理员会在微信端收到一条邀请通知，点开消息之后点击确认就行，如下图：</h4><span id="more"></span>

<h4 id="上一步之后公众号关联小程序已经完成，接下来是如何在小程序里关注公众号，在小程序后台需要打开公众号关注组件的开关："><a href="#上一步之后公众号关联小程序已经完成，接下来是如何在小程序里关注公众号，在小程序后台需要打开公众号关注组件的开关：" class="headerlink" title="上一步之后公众号关联小程序已经完成，接下来是如何在小程序里关注公众号，在小程序后台需要打开公众号关注组件的开关："></a>上一步之后公众号关联小程序已经完成，接下来是如何在小程序里关注公众号，在小程序后台需要打开公众号关注组件的开关：</h4><img src="/2018/11/28/weapp/beiyaoqing.png" class="" title="beiyaoqing">
<h4 id="直接在页面上使用这个组件即可，效果如下："><a href="#直接在页面上使用这个组件即可，效果如下：" class="headerlink" title="直接在页面上使用这个组件即可&lt;official-account&gt;&lt;/official-account&gt;，效果如下："></a>直接在页面上使用这个组件即可<code>&lt;official-account&gt;&lt;/official-account&gt;</code>，效果如下：</h4>
<h4 id="要注意的是这个组件只有在下面四个场景下才能展示，其中第三个场景是用户使用较多的，但是如果小程序在后台放置一段时间后会被销毁，再次打开也不会看到，只能从新扫描二维码，还是比较鸡肋的。"><a href="#要注意的是这个组件只有在下面四个场景下才能展示，其中第三个场景是用户使用较多的，但是如果小程序在后台放置一段时间后会被销毁，再次打开也不会看到，只能从新扫描二维码，还是比较鸡肋的。" class="headerlink" title="要注意的是这个组件只有在下面四个场景下才能展示，其中第三个场景是用户使用较多的，但是如果小程序在后台放置一段时间后会被销毁，再次打开也不会看到，只能从新扫描二维码，还是比较鸡肋的。"></a>要注意的是这个组件只有在下面四个场景下才能展示，其中第三个场景是用户使用较多的，但是如果小程序在后台放置一段时间后会被销毁，再次打开也不会看到，只能从新扫描二维码，还是比较鸡肋的。</h4><img src="/2018/11/28/weapp/changjing.png" class="" title="changjing">

<hr>
<h2 id="小程序的开发版和体验版"><a href="#小程序的开发版和体验版" class="headerlink" title="小程序的开发版和体验版"></a>小程序的开发版和体验版</h2><h4 id="开发版："><a href="#开发版：" class="headerlink" title="开发版："></a>开发版：</h4><p>扫描开发者工具里预览的二维码打开的小程序就是开发版，每次修改完代码之后需要点击预览生成一个新的二维码查看，用户需要在后台配置开发者权限。</p>
<h4 id="体验版："><a href="#体验版：" class="headerlink" title="体验版："></a>体验版：</h4><p>体验版相对于开发版的好处是只需扫描一个二维码，之后在最近使用中打开即可。修改了代码之后上传代码，也会自动覆盖之前的，不需要再次扫描二维码，方法如下：</p>
<ol>
<li>首先需要在开发者工具里输入小程序的appid（创建项目的时候可以填，创建好的项目在详情里也可以修改）进行关联，接着上传代码：<img src="/2018/11/28/weapp/upload.png" class="" title="upload"></li>
<li>上传成功之后在小程序后台的开发管理就能看到上传记录，点击选为体验版本：<img src="/2018/11/28/weapp/tiyan.png" class="" title="tiyan"></li>
<li>用户需要在后台配置体验者权限，扫描体验版二维码即可。</li>
</ol>
]]></content>
      <categories>
        <category>编程</category>
      </categories>
      <tags>
        <tag>小程序</tag>
      </tags>
  </entry>
  <entry>
    <title>vscode 代码格式化设置</title>
    <url>/2024/07/15/vscodeFormat/</url>
    <content><![CDATA[<h4 id="1-设置默认格式化工具"><a href="#1-设置默认格式化工具" class="headerlink" title="1.设置默认格式化工具"></a>1.设置默认格式化工具</h4><h5 id="cmd-shift-P，筛选“Format-Document”，设置-Prettier-为默认"><a href="#cmd-shift-P，筛选“Format-Document”，设置-Prettier-为默认" class="headerlink" title="cmd+shift+P，筛选“Format Document”，设置 Prettier 为默认"></a>cmd+shift+P，筛选“Format Document”，设置 Prettier 为默认</h5><img src="/2024/07/15/vscodeFormat/img0.png" class="">

<img src="/2024/07/15/vscodeFormat/img1.png" class="">

<h4 id="2-设置保存自动格式化"><a href="#2-设置保存自动格式化" class="headerlink" title="2.设置保存自动格式化"></a>2.设置保存自动格式化</h4><ul>
<li>打开 VSCode，并打开你想要格式化的代码文件。</li>
<li>在菜单栏中，选择“文件”&gt;“首选项”&gt;“设置”（快捷键 Ctrl+,）。</li>
<li>在搜索框中输入“format on save”，然后点击“编辑 in settings.json”选项。</li>
<li>在打开的“settings.json”文件中，找到“editor.formatOnSave”选项，并将其设置为“true”。</li>
<li>保存“settings.json”文件，并关闭它。</li>
</ul>
<img src="/2024/07/15/vscodeFormat/img2.png" class="">
]]></content>
      <categories>
        <category>编程</category>
      </categories>
      <tags>
        <tag>vscode</tag>
      </tags>
  </entry>
  <entry>
    <title>如何通过url跳转到微信公众号关注页</title>
    <url>/2018/05/24/wechatFollow/</url>
    <content><![CDATA[<h2 id="微信端"><a href="#微信端" class="headerlink" title="微信端"></a>微信端</h2><h4 id="微信端实现相对比较简单，首先需要获取公众号关注页的url，步骤如下："><a href="#微信端实现相对比较简单，首先需要获取公众号关注页的url，步骤如下：" class="headerlink" title="微信端实现相对比较简单，首先需要获取公众号关注页的url，步骤如下："></a>微信端实现相对比较简单，首先需要获取公众号关注页的url，步骤如下：</h4><p>1.点击公众号的历史消息</p>

<span id="more"></span>
<p>2.复制链接</p>

<p>3.接下来在微信里直接跳转这个链接即可</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">window</span>.<span class="property">location</span>.<span class="property">href</span>=<span class="string">&quot;https://mp.weixin.qq.com/mp/profile_ext?action=home&amp;__biz=MzAxNjU0MjEyNA==&amp;scene=123&amp;from=singlemessage&amp;isappinstalled=0#wechat_redirect&quot;</span></span><br></pre></td></tr></table></figure>
<h2 id="手机QQ"><a href="#手机QQ" class="headerlink" title="手机QQ"></a>手机QQ</h2><p>直接跳转这个链接即可唤起微信：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">window</span>.<span class="property">location</span>.<span class="property">href</span> = <span class="string">&#x27;weixin://&#x27;</span>;</span><br></pre></td></tr></table></figure>
<p>但是不能在QQ里跳转微信的那个链接，如下图</p>
<img src="/2018/05/24/wechatFollow/qq_error.png" class="" title="qq_error">

<h2 id="平台的判断"><a href="#平台的判断" class="headerlink" title="平台的判断"></a>平台的判断</h2><p>这里用的到是userAgent，代码如下</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> ua = navigator.<span class="property">userAgent</span>.<span class="title function_">toLowerCase</span>();</span><br><span class="line"><span class="keyword">if</span> (ua.<span class="title function_">indexOf</span>(<span class="string">&quot;micromessenger&quot;</span>) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">//微信端</span></span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (ua.<span class="title function_">indexOf</span>(<span class="string">&quot;qq&quot;</span>) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">//手机qq</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>编程</category>
      </categories>
      <tags>
        <tag>js</tag>
      </tags>
  </entry>
</search>
